# Set the path-simplification tolerance in device pixels (0 disables).

Set the path-simplification tolerance in device pixels (0 disables).

## Usage

``` r
rs_set_simplify_tol(tol)
```

## Arguments

- tol:

  Tolerance in device px.
