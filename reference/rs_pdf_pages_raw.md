# Several scenes as the pages of one PDF, returned as raw bytes.

Several scenes as the pages of one PDF, returned as raw bytes.

## Usage

``` r
rs_pdf_pages_raw(scenes)
```

## Arguments

- scenes:

  A list of compiled scenes.

## Value

A list of `bytes` and `warnings`.
