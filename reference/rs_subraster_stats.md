# Sub-raster cache stats: `c(hits, misses, resident_entries)` (tests/diagnostics).

Sub-raster cache stats: `c(hits, misses, resident_entries)`
(tests/diagnostics).

## Usage

``` r
rs_subraster_stats()
```
