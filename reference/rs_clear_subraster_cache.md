# Empty the repaint-boundary sub-raster cache (FW4c) and reset its counters.

Empty the repaint-boundary sub-raster cache (FW4c) and reset its
counters.

## Usage

``` r
rs_clear_subraster_cache()
```
