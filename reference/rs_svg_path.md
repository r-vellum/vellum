# Parse SVG path data into flattened rings.

Parse SVG path data into flattened rings.

## Usage

``` r
rs_svg_path(d)
```

## Arguments

- d:

  The `d` attribute of an SVG `<path>`.

## Value

List of `x`, `y`, `nper`, `closed`.
