% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pick.R
\name{element_geometry}
\alias{element_geometry}
\title{The true geometry of a scene's addressable elements}
\usage{
element_geometry(scene)
}
\arguments{
\item{scene}{A \code{\link[=vl_scene]{vl_scene()}} (or anything with an \code{\link[=as_vellum_scene]{as_vellum_scene()}} method).}
}
\value{
A data frame of \code{key}, \code{kind}, \code{vertex} (1-based within the element),
\code{x} and \code{y}. Zero rows if the scene has no keyed elements.
}
\description{
Returns the resolved device-pixel geometry of every \strong{keyed} element: the two
endpoints of a segment, the vertices of a line or polygon, the centre of a
round mark, the opposite corners of a rect.
}
\details{
This is what a host needs to hit-test \emph{exactly} without asking the engine per
event. \code{\link[=scene_model]{scene_model()}} gives bounding boxes, which are right for a brush and
misleading for diagonal or thin marks; this gives the shape the box was
standing in for, once, so a browser can compute point-to-geometry distances
locally at whatever rate it likes.
}
\section{Reading the result}{

One row per vertex, grouped by \code{key}. What the vertices mean depends on
\code{kind}:\tabular{lll}{
   \code{kind} \tab rows \tab meaning \cr
   \code{segment} \tab 2 \tab the endpoints \cr
   \code{line}, \code{polygon} \tab \emph{k} \tab the vertices, in order; \code{polygon} is closed \cr
   \code{path} \tab \emph{k} \tab all rings' vertices concatenated \cr
   \code{point} \tab 1 \tab the centre \cr
   \code{rect} \tab 2 \tab opposite corners \cr
   \code{text}, \code{roundrect} \tab 2 \tab the bounding box's corners \cr
}


Coordinates are device pixels with y growing \strong{down}, matching
\code{\link[=scene_model]{scene_model()}}'s boxes and the rendered SVG's coordinate system.
}

\examples{
s <- vl_scene(4, 3, dpi = 96, bg = "white") |>
  draw(segments_grob(0.1, 0.1, 0.9, 0.9, key = "diagonal"))
element_geometry(s)
}
\seealso{
\code{\link[=vl_nearest]{vl_nearest()}}, \code{\link[=scene_model]{scene_model()}}
}
