% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/debug.R
\name{why_size}
\alias{why_size}
\title{Explain why a node has its resolved size}
\usage{
why_size(scene, name)
}
\arguments{
\item{scene}{A \code{\link[=vl_scene]{vl_scene()}} (or anything with an \code{\link[=as_vellum_scene]{as_vellum_scene()}} method).}

\item{name}{A node name (set via the \code{name} argument of a viewport/grob).}
}
\value{
A \code{vellum_why_size} record (a list with \code{name}, \code{width_mm},
\code{height_mm}, and \code{determined_by}), printed legibly.
}
\description{
Reports the resolved width and height of a named viewport (or grob) and what
determined them — the layout track it was placed in, or the units of its own
width/height. The layout companion to the visual \code{render(scene, debug = TRUE)}
overlay; together they make coordinate debugging first-class rather than an
exercise in archaeology.
}
\examples{
s <- vl_scene(4, 3) |>
  push(viewport(name = "panel", width = unit(2, "in"), height = unit(1, "in")))
why_size(s, "panel")
}
