% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{Scene}
\alias{Scene}
\title{A drawing scene held in the Rust backend. Internal: the public R API is the
S7 layer (\code{vl_scene()}, grobs, \code{render()}), which compiles to this object.}
\usage{
Scene
}
\description{
A drawing scene held in the Rust backend. Internal: the public R API is the
S7 layer (\code{vl_scene()}, grobs, \code{render()}), which compiles to this object.
}
\section{Methods}{

\subsection{Method \code{new}}{
Create a scene \code{width} x \code{height} inches at \code{dpi}, with background \code{bg}
(a length-4 integer RGBA vector). A root viewport covering the whole page
(npc == native) is created as viewport 0.
}

\subsection{Method \code{set_pick}}{
Set the hit-test pick id applied to subsequently-emitted primitives (one
per grob; the R side assigns ids in paint order). See \code{hit_test}.
}

\subsection{Method \code{push_viewport}}{
Push a viewport as a child of the current one and make it current.
Returns the new viewport's id. If \code{lrow}/\code{lcol} are >= 0 the viewport is
placed into that (0-based) cell of the parent's layout; otherwise it is
placed by centre/size in parent coordinates.
}

\subsection{Method \code{set_clip_path}}{
Attach an arbitrary clip path (in the current viewport's coordinates) to
the current viewport; \code{nper} gives the point count of each closed sub-path.
}

\subsection{Method \code{pop_viewport}}{
Move the cursor up \code{n} levels (towards the root). Stops at the root.
}

\subsection{Method \code{to_root}}{
Move the cursor to the root viewport.
}

\subsection{Method \code{set_layout}}{
Attach a row/column layout to the current viewport. Tracks are given as
parallel value + unit-code vectors; the unit \code{"null"} marks a flexible
track whose value is its weight. \code{respect} enables grid-style aspect
locking (equal physical size for a \code{null} width unit and a \code{null} height
unit; see \code{solve_layout}).
}

\subsection{Method \code{rects}}{
A whole batch of rectangles in one call, sharing one gpar. Coordinates and
per-coordinate unit codes are parallel slices.
}

\subsection{Method \code{circles}}{
A whole batch of circles in one call, sharing one gpar (also used for
\code{points}, with the radius carrying the marker size).
}

\subsection{Method \code{markers}}{
A batch of markers (point glyphs) sharing one gpar. Like \code{circles} but each
element carries a \code{shape} code (0 circle, 1 square, 2 triangle, 3 diamond,
4 plus, 5 cross); \code{size} is the marker radius. Filled shapes fill+stroke per
gpar; plus/cross are stroke-only. (circle_grob / default points use the
faster \code{circles} path; this is for shape variety.)
}

\subsection{Method \code{hexagons}}{
A batch of regular hexagons for hex-binning. \code{size} is the circumradius;
\code{flat} picks flat-top vs pointy-top; \code{fill} is a flat per-hex RGBA stream
(\verb{[r,g,b,a, r,g,b,a, ...]}, one quad per hex). \code{col}/\code{lwd}/\code{alpha}/\code{stroke}
give the \emph{uniform} stroke (the gpar's fill is unused — fill is per element).
}

\subsection{Method \code{segments}}{
A batch of disjoint line segments (stroke only), sharing one gpar.
}

\subsection{Method \code{path}}{
A general path. \code{nper} gives the number of points in each closed sub-path
(so holes are sub-paths under the even-odd or winding rule).
}

\subsection{Method \code{image}}{
A raster image: \code{rgba} is a flat straight-RGBA integer vector (\code{iw} x \code{ih},
top-left, 4 per pixel), drawn into a \code{w} x \code{h} cell centred at \verb{(x, y)}.
}

\subsection{Method \code{text}}{
Add pre-shaped text. The R wrapper does shaping (via \code{textshaping}) and
passes per-glyph ids/positions/fonts plus the block size.
}

\subsection{Method \code{texts}}{
Add a whole batch of pre-shaped text labels in one call (one shaping pass
on the R side, one FFI here). Glyphs are flat across all labels, split by
\code{nper} (glyph count per label); positions/sizes/rot/labels are per-label;
font + just + colour are shared.
}

\subsection{Method \code{mask_begin}}{
Begin collecting a mask's content for the current viewport. Until the
matching \code{mask_end}, primitives are routed into the mask instead of the
drawn scene. \code{kind} is 0 (alpha) or 1 (luminance). Returns its index.
}

\subsection{Method \code{mask_end}}{
Stop routing primitives into the most recent mask.
}

\subsection{Method \code{group_start}}{
Open an isolated compositing group, modulated by mask index \code{mask}
(negative = no mask, just isolation), group opacity \code{alpha}, and blend mode
\code{blend} (a code; 0 = normal). Routed through \code{emit_node} so a group nested
inside a mask (a mask grob that itself masks a viewport) lands in the same
node list as its content, keeping markers and content in sync.
}

\subsection{Method \code{group_end}}{
Close the most recently opened group.
}

\subsection{Method \code{len}}{
Number of primitives currently in the scene.
}

\subsection{Method \code{dim}}{
Device dimensions in pixels, \code{c(width, height)}.
}

\subsection{Method \code{dpi}}{
Device resolution in dots per inch.
}

\subsection{Method \code{render_png}}{
Render the scene to a PNG file.
}

\subsection{Method \code{render_svg}}{
Render the scene to an SVG file. \code{outline_text} emits glyph outlines
instead of selectable \verb{<text>} (pixel-faithful, matches raster/PDF).
}

\subsection{Method \code{render_pdf}}{
Render the scene to a PDF file.
}

\subsection{Method \code{rgba}}{
Render and return the whole image as row-major RGBA bytes
\verb{[r, g, b, a, ...]} (top-left origin, x fastest).
}

\subsection{Method \code{content_bbox}}{
Render and return the tight bounding box of non-transparent content as
\code{c(min_x, min_y, max_x, max_y)} (device px, inclusive), or an empty vector
if nothing was drawn. Used to measure a grob's extent (grobwidth/height).
}

\subsection{Method \code{pixel}}{
Render and return the RGBA of device pixel \verb{(x, y)} as \code{c(r, g, b, a)}.
}

\subsection{Method \code{hit_test}}{
Hit-test: return the pick id of the topmost primitive covering device pixel
\verb{(x, y)}, or -1 if none. Implemented as a colour pick-buffer — each node is
drawn opaque (AA off) in a colour encoding its pick id, respecting clips and
paint order, then the pixel is decoded — so it is geometry/clip/overlap
exact. Markers and text use a bounding box; lines/segments a pick band.
}
}

