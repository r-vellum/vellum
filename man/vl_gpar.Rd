% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpar.R
\name{vl_gpar}
\alias{vl_gpar}
\title{Graphical parameters}
\usage{
vl_gpar(
  col = NULL,
  fill = NULL,
  lwd = NULL,
  alpha = NULL,
  lty = NULL,
  lineend = NULL,
  linejoin = NULL,
  linemitre = NULL,
  fontfamily = NULL,
  fontface = NULL,
  fontsize = NULL,
  cex = NULL,
  lineheight = NULL,
  halo_col = NULL,
  halo_width = NULL,
  features = NULL,
  antialias = NULL,
  crisp = NULL,
  dash_phase = NULL
)
}
\arguments{
\item{col}{Stroke/text colour, or a gradient from \code{\link[=linear_gradient]{linear_gradient()}} /
\code{\link[=radial_gradient]{radial_gradient()}} to \strong{stroke with a gradient} — the same paint model as
\code{fill}, applied to the stroked region instead of the enclosed one. A
gradient here affects stroked paths; text and markers fall back to its first
stop, since a glyph run has no path to run a ramp along.}

\item{fill}{Fill colour, or a gradient from \code{\link[=linear_gradient]{linear_gradient()}} /
\code{\link[=radial_gradient]{radial_gradient()}}.}

\item{lwd}{Line width (1 == 1/96 inch).}

\item{alpha}{Opacity multiplier in \verb{[0, 1]}.}

\item{lty}{Line type: a name (\code{"solid"}, \code{"dashed"}, \code{"dotted"}, \code{"dotdash"},
\code{"longdash"}, \code{"twodash"}), an integer code \code{0:6}, a hex dash string (e.g.
\code{"44"}), or a numeric vector of on/off dash lengths. Dash lengths scale with
\code{lwd}.}

\item{lineend}{Line cap: \code{"round"} (default), \code{"butt"}, or \code{"square"}.}

\item{linejoin}{Line join: \code{"round"} (default), \code{"mitre"}, or \code{"bevel"}.}

\item{linemitre}{Mitre limit (>= 1) for mitre joins; default 10.}

\item{fontfamily}{Font family (text grobs).}

\item{fontface}{One of \code{"plain"}, \code{"bold"}, \code{"italic"}, \code{"bold.italic"}.}

\item{fontsize}{Font size in points.}

\item{cex}{Multiplier applied to \code{fontsize} (grid semantics), so a theme can
ask for a relative size without knowing the base one. \code{cex = 2} is exactly
equivalent to doubling \code{fontsize}: it scales drawn text, \code{char}/\code{line}
units, and \code{\link[=grobwidth]{grobwidth()}}/\code{\link[=grobheight]{grobheight()}} measurement alike. \code{NULL} (the
default) means 1.}

\item{lineheight}{Line-height multiple.}

\item{halo_col}{Halo colour for text (a "shadowtext" outline drawn \emph{under}
the glyphs, so a label stays legible over dense marks or map imagery).
\code{NULL} (default) means no halo. Needs \code{halo_width} to be visible.}

\item{halo_width}{Halo thickness in points -- the visible width outside the
glyph. \code{NULL} or \code{0} means no halo. A good starting point is roughly an
eighth of \code{fontsize}.}

\item{features}{OpenType font features, as a named numeric vector of
four-character feature tags -- e.g. \code{c(tnum = 1)} for tabular (fixed-width)
figures so axis labels stop jittering between ticks, \code{c(smcp = 1)} for small
caps, \code{c(onum = 1)} for oldstyle figures, \code{c(liga = 0)} to switch ligatures
off, or \code{c(kern = 0)} to disable kerning. \code{NULL} (default) uses the font's
own defaults. A feature the font does not carry is silently ignored --
that is HarfBuzz's behaviour, not something vellum can check for you.}

\item{antialias}{Anti-alias this element's edges? \code{NULL} (default) inherits;
the root default is \code{TRUE}. \code{FALSE} gives hard pixel edges, which is what
pixel art, QR codes, and heatmap cells that must tile without a seam want.}

\item{crisp}{Snap axis-parallel strokes onto the pixel grid? \code{NULL} (default)
inherits; the root default is \code{FALSE}. A 1-px rule at a fractional
coordinate straddles two pixel rows and renders as two grey ones rather than
one solid — the reason gridlines look muddy on screen. \code{TRUE} snaps
horizontal and vertical runs so they land on whole pixels. Diagonals are
unaffected (there is no grid to snap them to), and it only applies to raster
output — a vector format has no pixel grid.}

\item{dash_phase}{How far into the dash pattern a dashed line starts, as a
multiple of \code{lwd} (so it scales with the line width exactly as the dash
nibbles do). Use it to line up dashes across adjacent strokes, or animate it
for marching ants. \code{NULL} (default) means 0. Ignored for a solid line.}
}
\value{
A \code{gpar} object.
}
\description{
Builds a set of graphical parameters attached to a grob or viewport. Any field
left \code{NULL} is inherited from the enclosing viewport; \code{alpha} multiplies down
the viewport tree. A colour value sets it; \code{NA} means "no paint".
}
\examples{
vl_gpar(col = "steelblue", lwd = 2, lty = "dashed", lineend = "round")
}
