% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{vl_strwidth}
\alias{vl_strwidth}
\alias{vl_strheight}
\title{Measure text}
\usage{
vl_strwidth(
  label,
  family = "",
  fontface = "plain",
  fontsize = 12,
  cex = 1,
  unit = "in"
)

vl_strheight(
  label,
  family = "",
  fontface = "plain",
  fontsize = 12,
  cex = 1,
  unit = "in"
)
}
\arguments{
\item{label}{Character vector of strings to measure.}

\item{family}{Font family (e.g. \code{"sans"}, \code{"serif"}, \code{"mono"}, or a specific
family name). \code{""} uses the system default.}

\item{fontface}{One of \code{"plain"}, \code{"bold"}, \code{"italic"}, \code{"bold.italic"}.}

\item{fontsize}{Font size in points.}

\item{cex}{Multiplier applied to \code{fontsize}.}

\item{unit}{Output unit: one of \code{"in"}, \code{"pt"}, \code{"mm"}, \code{"cm"}.}
}
\value{
A numeric vector (one per \code{label}) of widths/heights in \code{unit}.
}
\description{
\code{vl_strwidth()} / \code{vl_strheight()} return the rendered width/height of each
string, using the same shaping (\pkg{textshaping}/HarfBuzz + \pkg{systemfonts})
the renderer uses, so measurements match drawn text. Device-independent (does
not need an open scene). Vectorised over \code{label}. (Named \verb{vl_*} to avoid masking
\code{grDevices::strwidth()}.)
}
\examples{
vl_strwidth(c("short", "a longer label"), fontsize = 14)
}
