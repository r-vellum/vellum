% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datashade.R
\name{datashade_lines}
\alias{datashade_lines}
\alias{datashade_segments}
\title{Aggregate-then-shade dense lines and segments (datashader-style)}
\usage{
datashade_lines(
  x,
  y,
  group = NULL,
  weight = NULL,
  width = 600L,
  height = 400L,
  xlim = NULL,
  ylim = NULL,
  colors = c("#deebf7", "#08306b"),
  how = c("eq_hist", "log", "cbrt", "linear"),
  span = NULL,
  clip = NULL,
  spread = NULL,
  interpolate = FALSE,
  name = NULL,
  vp = NULL,
  id = NULL,
  role = NULL
)

datashade_segments(
  x0,
  y0,
  x1,
  y1,
  weight = NULL,
  width = 600L,
  height = 400L,
  xlim = NULL,
  ylim = NULL,
  colors = c("#deebf7", "#08306b"),
  how = c("eq_hist", "log", "cbrt", "linear"),
  span = NULL,
  clip = NULL,
  spread = NULL,
  interpolate = FALSE,
  name = NULL,
  vp = NULL,
  id = NULL,
  role = NULL
)
}
\arguments{
\item{x, y}{For \code{datashade_lines()}, the polyline vertices (data space); a
segment joins each consecutive pair.}

\item{group}{For \code{datashade_lines()}, an optional per-vertex series id (factor
or vector) the same length as \code{x}. The line breaks between vertices whose
group differs, so multiple series pack into one call. \code{NULL} treats all
vertices as one series (still broken by \code{NA} coordinates).}

\item{weight}{Optional per-line weight (per start-vertex for
\code{datashade_lines()}, per segment for \code{datashade_segments()}): cells
accumulate summed weight instead of plain coverage. \code{NULL} weighs each line 1;
a scalar is recycled; otherwise it must match the line/segment count.}

\item{width, height}{Aggregation grid size in cells (= output raster pixels).}

\item{xlim, ylim}{Data range to bin over; default the finite range of \code{x}/\code{y}.}

\item{colors}{For density shading, two or more colours forming the low-to-high
ramp. For categorical shading (\code{category} set), a per-category hue vector —
named by category level, or one colour per level in level order.}

\item{how}{Density-to-colour mapping: \code{"eq_hist"} (histogram equalisation —
datashader's default, reveals structure across orders of magnitude), \code{"log"},
\code{"cbrt"} (cube root), or \code{"linear"}. Also drives the per-cell opacity under
categorical shading.}

\item{span}{Optional \code{c(lo, hi)} density values mapped to the ends of the
colour ramp / opacity range; densities outside are clamped. \code{NULL} (default)
uses the full observed range.}

\item{clip}{Optional percentile pair in \verb{[0, 1]} (e.g. \code{c(0.01, 0.99)}) deriving
\code{span} from the quantiles of the non-empty cell densities — a robust way to
keep a few extreme cells from flattening the rest. Overrides \code{span}.}

\item{spread}{Optional post-aggregation spreading, applied to the shaded
raster to keep sparse output visible (see \code{\link[=spread]{spread()}} / \code{\link[=dynspread]{dynspread()}}):
\code{NULL} (default) none; a positive integer applies \code{\link[=spread]{spread()}} with that pixel
radius; \code{"auto"} applies \code{\link[=dynspread]{dynspread()}} (radius chosen from the image density).}

\item{interpolate}{Passed to \code{\link[=raster_grob]{raster_grob()}}; \code{FALSE} keeps hard bin edges.}

\item{name, vp, id, role}{Passed to \code{\link[=raster_grob]{raster_grob()}} (see \link{grob}).}

\item{x0, y0, x1, y1}{For \code{datashade_segments()}, the segment endpoints (data
space), one per segment; all four the same length.}
}
\value{
A \link{grob} (a raster), drawable with \code{\link[=draw]{draw()}}.
}
\description{
The line/segment analogue of \code{\link[=datashade]{datashade()}}. Past a few hundred thousand line
vertices — a dense stack of timeseries, or the edges of a large graph — drawing
each line as a vector primitive overplots into a solid mass and balloons the
output. \code{datashade_lines()} and \code{datashade_segments()} instead \strong{rasterise} the
lines into a canvas-sized grid in one pass: each cell accumulates the
(anti-aliased) coverage of the lines crossing it, so overlapping lines \emph{add} and
the grid records true line density. The grid is shaded exactly like
\code{\link[=datashade]{datashade()}} (\code{colors}/\code{how}/\code{span}/\code{clip}) and returned as a single
\code{\link[=raster_grob]{raster_grob()}}.
}
\details{
\itemize{
\item \code{datashade_lines()} takes a \strong{connected polyline}: a segment is drawn between
each consecutive \verb{(x, y)}. Pass \code{group} to pack several series into one call —
the line breaks wherever the group changes; an \code{NA} in \code{x}/\code{y} also breaks it.
This is the dense-timeseries path.
\item \code{datashade_segments()} takes \strong{independent segments} \verb{(x0, y0) -> (x1, y1)},
one per element. This is the network-edge / \code{mark_segment} path.
}

Line coverage is anti-aliased (a Wu accumulator) and summed, so a line deposits
roughly \code{weight} per cell it spans and dense bundles brighten honestly rather
than saturating. As with \code{\link[=datashade]{datashade()}}, align the raster to data axes by drawing
it in a \code{\link[=vl_viewport]{vl_viewport()}} whose \code{xscale}/\code{yscale} match \code{xlim}/\code{ylim}.
}
\examples{
set.seed(1)
# Dense timeseries: 400 random walks of 500 steps, packed into one raster.
k <- 400; m <- 500
walks <- apply(matrix(rnorm(k * m), m, k), 2, cumsum)
t <- rep(seq_len(m), k)
g <- datashade_lines(t, as.vector(walks), group = rep(seq_len(k), each = m),
                     width = 400, height = 300)

# Network edges: random segments shaded by edge density.
n <- 5000
e <- datashade_segments(rnorm(n), rnorm(n), rnorm(n), rnorm(n))
}
\seealso{
\code{\link[=datashade]{datashade()}} for points; \code{\link[=dynspread]{dynspread()}}/\code{\link[=spread]{spread()}} for keeping thin
lines visible.
}
