% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{scene_stats}
\alias{scene_stats}
\title{Ink and overplotting statistics for a scene}
\usage{
scene_stats(scene)
}
\arguments{
\item{scene}{A \code{\link[=vl_scene]{vl_scene()}}, or anything with an \code{\link[=as_vellum_scene]{as_vellum_scene()}} method.}
}
\value{
A one-row data frame:
\describe{
\item{\code{elements}}{total drawn elements.}
\item{\code{ink}}{fraction of canvas pixels differing from the background.}
\item{\code{colours}}{distinct colours in the rendered image.}
\item{\code{overplot}}{summed element-box area divided by inked area — an
estimate of how many times an average inked pixel was drawn over. \code{1}
means no overlap. It is computed from bounding boxes, so it
over-estimates for non-rectangular marks (a circle fills ~79\% of its
box); treat it as an index, not a measurement. Marks the element table
does not cover — text, and unkeyed lines/polygons/paths — contribute to
\code{ink} but not to \code{overplot}.}
}
}
\description{
How much of the canvas a scene actually covers, and how hard it is working to
do it. \code{overplot} is the honest signal for "should this be
\code{\link[=datashade]{datashade()}}-ed?": a count of marks says nothing about whether they overlap,
whereas thirty thousand well-separated points are fine and eight thousand
piled on top of each other are not.
}
\examples{
set.seed(1)
sparse <- vl_scene(4, 3) |> draw(points_grob(runif(200), runif(200)))
dense <- vl_scene(4, 3) |> draw(points_grob(rnorm(20000, 0.5, 0.05),
                                            rnorm(20000, 0.5, 0.05)))
rbind(sparse = scene_stats(sparse), dense = scene_stats(dense))
}
\seealso{
\code{\link[=vl_lint]{vl_lint()}}, \code{\link[=profile_render]{profile_render()}}, \code{\link[=datashade]{datashade()}}
}
