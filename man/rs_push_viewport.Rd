% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scene.R
\name{rs_push_viewport}
\alias{rs_push_viewport}
\alias{rs_pop_viewport}
\alias{rs_up_viewport}
\title{Push, pop, and navigate viewports}
\usage{
rs_push_viewport(
  scene,
  x = 0.5,
  y = 0.5,
  width = 1,
  height = 1,
  units = "npc",
  xscale = c(0, 1),
  yscale = c(0, 1),
  angle = 0,
  clip = FALSE,
  gp = rs_gpar(),
  row = NULL,
  col = NULL,
  rowspan = 1,
  colspan = 1
)

rs_pop_viewport(scene, n = 1)

rs_up_viewport(scene, n = 1)
}
\arguments{
\item{scene}{A \code{\link[=rs_scene]{rs_scene()}}.}

\item{x, y}{Viewport centre in parent coordinates (ignored if \code{row}/\code{col}).}

\item{width, height}{Viewport size in parent coordinates (ignored if
\code{row}/\code{col}).}

\item{units}{Coordinate system for the above; see \code{\link[=rs_rect]{rs_rect()}}.}

\item{xscale, yscale}{Length-2 native coordinate ranges for this viewport.}

\item{angle}{Rotation in degrees, counter-clockwise about the centre.}

\item{clip}{Clip drawing to this viewport's rectangle?}

\item{gp}{Inheritable graphical parameters, from \code{\link[=rs_gpar]{rs_gpar()}}.}

\item{row, col}{Cell position (1-based) within the parent's layout. If both are
given, the viewport is placed in that cell instead of by centre/size.}

\item{rowspan, colspan}{Number of cells to span.}

\item{n}{Number of levels to move up.}
}
\value{
\code{rs_push_viewport()} returns the new viewport's id; the others return
\code{scene}, invisibly.
}
\description{
Viewports form a tree. \code{rs_push_viewport()} creates a child of the current
viewport and makes it current; subsequent drawing goes into it. A viewport is
placed either by centre/size in its parent's coordinates, or — if \code{row}/\code{col}
are given — into a cell of the parent's \code{\link[=rs_layout]{rs_layout()}}. Viewports may rotate
(\code{angle}), clip their contents (\code{clip}), and set inheritable graphical
parameters (\code{gp}).
}
\keyword{internal}
