% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{scene_raster}
\alias{scene_raster}
\title{Read a rendered scene back as pixels}
\usage{
scene_raster(scene)
}
\arguments{
\item{scene}{A \code{\link[=vl_scene]{vl_scene()}} (or anything with an \code{\link[=as_vellum_scene]{as_vellum_scene()}} method).}
}
\value{
\code{scene_raster()}: an integer array of dimension \code{c(4, width, height)}.
The \code{as.raster()} method: a \code{raster} (character matrix, \code{c(height, width)}).
}
\description{
\code{scene_raster()} renders \code{scene} and returns its pixels as an integer array
with dimensions \code{c(channel, x, y)} — RGBA channels in \code{0:255}, top-left origin,
\code{y} increasing downward. This is the form most convenient for probing or
testing (e.g. \code{scene_raster(s)[1, x, y]} is the red value at pixel \verb{(x, y)}).
}
\details{
An \code{\link[grDevices:as.raster]{grDevices::as.raster()}} method returns the same image as a \code{raster} object
(a character matrix of hex colours), drawable with \code{\link[graphics:plot]{graphics::plot()}} or
\code{\link[grid:rasterGrob]{grid::rasterGrob()}}.
}
\examples{
s <- vl_scene(2, 1, bg = "white") |>
  draw(circle_grob(r = 0.3, gp = vl_gpar(fill = "red", col = NA)))
dim(scene_raster(s)) # c(4, width_px, height_px)
}
