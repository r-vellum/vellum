% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{vl_scene}
\alias{vl_scene}
\alias{push}
\alias{draw}
\alias{pop}
\alias{render}
\title{Build and render a scene}
\usage{
vl_scene(width = 6, height = 4, dpi = 96, bg = "white")

push(scene, vp)

draw(scene, grob)

pop(scene, n = 1)

render(scene, path)
}
\arguments{
\item{width, height}{Page size (\code{\link[=unit]{unit()}} or numeric inches).}

\item{dpi}{Resolution in dots per inch.}

\item{bg}{Background colour (or \code{NA} for transparent).}

\item{scene}{A \code{\link[=vl_scene]{vl_scene()}}.}

\item{vp}{A \code{\link[=viewport]{viewport()}}.}

\item{grob}{A grob (see \link{grob}).}

\item{n}{Number of viewport levels to ascend.}

\item{path}{Output PNG file path.}
}
\value{
\code{vl_scene()}, \code{push()}, \code{draw()}, \code{pop()}: a \code{vellum_scene}.

\code{render()}: \code{path}, invisibly.
}
\description{
\code{vl_scene()} creates an empty scene. \code{\link[=push]{push()}} adds a viewport (and descends
into it), \code{\link[=draw]{draw()}} adds a grob, \code{\link[=pop]{pop()}} ascends. All return a new scene value
(the scene is immutable). \code{\link[=render]{render()}} compiles the scene and writes a PNG.
}
\examples{
s <- vl_scene(width = 4, height = 3) |>
  push(viewport(xscale = c(0, 10), yscale = c(0, 10))) |>
  draw(rect_grob(gp = gpar(fill = "grey95", col = "grey50"))) |>
  draw(lines_grob(x = unit(0:10, "native"), y = unit(0:10, "native"),
                  gp = gpar(col = "steelblue", lwd = 2)))
}
