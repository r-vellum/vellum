% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{vl_scene}
\alias{vl_scene}
\alias{push}
\alias{draw}
\alias{pop}
\alias{render}
\title{Build and render a scene}
\usage{
vl_scene(
  width = 6,
  height = 4,
  dpi = 96,
  bg = "white",
  gp = vl_gpar(),
  xscale = c(0, 1),
  yscale = c(0, 1),
  clip = FALSE,
  title = NULL,
  desc = NULL
)

push(scene, vp)

draw(scene, grob)

pop(scene, n = 1)

render(scene, path, text = c("native", "outline"), debug = FALSE)
}
\arguments{
\item{width, height}{Page size (\code{\link[=vl_unit]{vl_unit()}} or numeric inches).}

\item{dpi}{Resolution in dots per inch.}

\item{bg}{Background colour (or \code{NA} for transparent).}

\item{gp}{Page-level graphical parameters (\code{\link[=vl_gpar]{vl_gpar()}}) carried by the root
viewport; inherited by everything drawn (e.g. a default \code{col}/\code{fontsize}).}

\item{xscale, yscale}{Native coordinate range of the root viewport, so
\code{"native"} units work at the page level without an explicit \code{\link[=push]{push()}}.}

\item{clip}{Clip drawing to the page rectangle?}

\item{title, desc}{Accessibility: an accessible \strong{name} (a short title) and a
longer \strong{description} (alt text) for the scene. When either is set, the SVG
backend emits \code{role="img"} + \verb{<title>}/\verb{<desc>} (referenced by
\code{aria-labelledby}) and the PDF backend tags the page as a Figure with the
description as Alt text. \code{NULL} (default) emits no accessibility markup, so
output is unchanged. See \code{\link[=describe]{describe()}} to set them on an existing scene.}

\item{scene}{A \code{\link[=vl_scene]{vl_scene()}}.}

\item{vp}{A \code{\link[=vl_viewport]{vl_viewport()}}.}

\item{grob}{A grob (see \link{grob}).}

\item{n}{Number of viewport levels to ascend.}

\item{path}{Output file path; the format is taken from the extension (\code{.png},
\code{.svg}, or \code{.pdf}).}

\item{text}{For SVG output, how text is written: \code{"native"} (default) emits
selectable \verb{<text>} referencing system fonts, \code{"outline"} emits glyph
outlines (pixel-faithful, identical to the raster/PDF backends, but not
selectable). Ignored for PNG/PDF.}

\item{debug}{If \code{TRUE}, overlay a layout-debug skeleton on the output: each
viewport region (outlined and labelled by name), its layout track boundaries,
and its clip region. Built from the resolved scene with \code{\link[=why_size]{why_size()}}; useful
for understanding why elements land where they do. Default \code{FALSE}.}
}
\value{
\code{vl_scene()}, \code{push()}, \code{draw()}, \code{pop()}: a \code{vellum_scene}.

\code{render()}: \code{path}, invisibly.
}
\description{
\code{vl_scene()} creates an empty scene. \code{\link[=push]{push()}} adds a viewport (and descends
into it), \code{\link[=draw]{draw()}} adds a grob, \code{\link[=pop]{pop()}} ascends. The builder is a linear pipe;
a \emph{rendered or edited} scene is an immutable value (\code{\link[=edit_node]{edit_node()}} copies on
modify). \code{\link[=render]{render()}} compiles the scene and writes the output.
}
\examples{
s <- vl_scene(width = 4, height = 3) |>
  push(vl_viewport(xscale = c(0, 10), yscale = c(0, 10))) |>
  draw(rect_grob(gp = vl_gpar(fill = "grey95", col = "grey50"))) |>
  draw(lines_grob(x = vl_unit(0:10, "native"), y = vl_unit(0:10, "native"),
                  gp = vl_gpar(col = "steelblue", lwd = 2)))
}
