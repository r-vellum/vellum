% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output.R
\name{render_all}
\alias{render_all}
\title{Render many scenes in parallel}
\usage{
render_all(scenes, paths, workers = NULL, ...)
}
\arguments{
\item{scenes}{A named or unnamed list of scenes.}

\item{paths}{Output paths, one per scene. The format of each comes from its
extension, exactly as in \code{\link[=render]{render()}}. When \code{scenes} is named and \code{paths} is a
single directory, files are named after the list.}

\item{workers}{Number of parallel workers. Defaults to one per available core,
capped at the number of scenes.}

\item{...}{Passed to \code{\link[=render]{render()}} for every scene.}
}
\value{
The paths written, invisibly.
}
\description{
Renders a list of independent scenes across cores. This is
embarrassingly parallel — one whole scene per worker, nothing shared — which
is why it exists while \emph{tiling a single raster} across threads does not:
that needs synchronised access to one pixmap and is declined in
\code{PERFORMANCE.md}.
}
\details{
The saving is real when the scenes are substantial and there are several of
them. For a handful of small figures the process/thread overhead dominates and
a plain \code{lapply(scenes, render)} is as fast; this reports enough to tell.
}
\examples{
scenes <- list(
  a = vl_scene(3, 2, dpi = 96) |> draw(circle_grob(gp = vl_gpar(fill = "tomato"))),
  b = vl_scene(3, 2, dpi = 96) |> draw(rect_grob(gp = vl_gpar(fill = "steelblue")))
)
render_all(scenes, file.path(tempdir(), c("a.png", "b.png")))
}
\seealso{
\code{\link[=render]{render()}}, \code{\link[=pdf_pages]{pdf_pages()}}
}
