% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{render_animation}
\alias{render_animation}
\title{Render a keyframe animation to \code{path}.}
\usage{
render_animation(
  keyframes,
  seg,
  frac,
  format,
  path,
  delay_num,
  delay_den,
  gif_speed,
  gif_dither
)
}
\description{
\itemize{
\item \code{keyframes} — a list of compiled \code{Scene} external pointers (the \code{K} states).
\item \code{seg} — per frame, the 0-based index of the frame's \strong{left} keyframe (the
right one is \code{seg + 1}).
\item \code{frac} — per frame, the eased interpolation fraction in \verb{[0, 1]}.
\item \code{format} — \code{"frames"} (a PNG per frame into directory \code{path}), \code{"apng"} (a
single animated PNG at \code{path}), or \code{"gif"} (a looping animated GIF).
\item \code{delay_num}/\code{delay_den} — per-frame delay as a fraction of a second (e.g.
\code{1}/\code{25} for 25 fps); rounded to centiseconds for GIF.
\item \code{gif_speed} — GIF only: NeuQuant palette sample factor, 1 (best) to 30
(fastest). Ignored by the other formats.
\item \code{gif_dither} — GIF only: apply Floyd–Steinberg dithering.
}
}
\details{
Returns any renderer degradation warnings (currently none for the raster path).
}
\keyword{internal}
