% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/place.R
\name{vl_empty_region}
\alias{vl_empty_region}
\title{Find the largest empty rectangle in a scene}
\usage{
vl_empty_region(
  scene,
  avoid = NULL,
  within = NULL,
  grid = 200,
  unit = c("npc", "mm")
)
}
\arguments{
\item{scene}{A \code{\link[=vl_scene]{vl_scene()}} (or anything with an \code{\link[=as_vellum_scene]{as_vellum_scene()}} method).}

\item{avoid}{Names of the grobs to treat as obstacles. \code{NULL} (default) takes
every node that is not a label.}

\item{within}{Region to search, as \code{c(x0, y0, x1, y1)} in \code{npc} of the page.
Defaults to the whole page.}

\item{grid}{Grid resolution (cells across the longer side).}

\item{unit}{Unit of the returned rectangle: \code{"npc"} (default) or \code{"mm"}.}
}
\value{
A named numeric \code{c(x0, y0, x1, y1)}, in \code{unit}, using the usual
y-grows-up convention. All zeros if there is no free space.
}
\description{
Answers "where is there room?" — for a legend, an annotation, a watermark, or
anything else that has to go somewhere the marks are not.
}
\details{
Occupancy is rasterised onto a grid and the answer is exact \emph{on that grid}.
The approximation is deliberate: the exact maximal empty rectangle over n
boxes is superquadratic, and nothing is placed to sub-pixel tolerance. Raise
\code{grid} to trade time for precision. Boxes are rounded outward, so the result
is conservative — it will never claim space that is in fact occupied.
}
\examples{
set.seed(1)
s <- vl_scene(4, 3, dpi = 96, bg = "white") |>
  draw(points_grob(runif(40) * 0.5, runif(40),
                   gp = vl_gpar(fill = "steelblue", col = NA)))
vl_empty_region(s)
}
