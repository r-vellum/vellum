% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paint.R
\name{as_mask}
\alias{as_mask}
\title{Masks}
\usage{
as_mask(grob, type = c("alpha", "luminance"))
}
\arguments{
\item{grob}{A grob, or a list of grobs, drawn in the masked viewport's
coordinate system.}

\item{type}{\code{"alpha"} (default) uses the mask's opacity as coverage;
\code{"luminance"} uses its brightness (white shows, black hides).}
}
\value{
A \code{vellum_mask} object.
}
\description{
Wrap a grob (or list of grobs) as a mask for \code{viewport(mask = ...)}. The mask
content is rendered to an isolated layer; its coverage then modulates the
visibility of the viewport's contents.
}
\examples{
as_mask(circle_grob(r = 0.4, gp = gpar(fill = "white", col = NA)))
}
