% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grob.R
\name{text_path_grob}
\alias{text_path_grob}
\title{Text set along a path}
\usage{
text_path_grob(
  label,
  x,
  y,
  just = "centre",
  offset = 0,
  gp = vl_gpar(),
  name = NULL,
  vp = NULL,
  id = NULL,
  role = NULL
)
}
\arguments{
\item{label}{A single character string.}

\item{x, y}{The baseline path, as unit vectors of equal length.}

\item{just}{Justification: \code{c(hjust, vjust)} as names (\code{"left"}, \code{"centre"},
\code{"right"}, \code{"bottom"}, \code{"top"}) or numbers in \verb{[0, 1]}.}

\item{offset}{Perpendicular standoff from the baseline in points; positive is
to the left of the direction of travel.}

\item{gp}{Graphical parameters, from \code{\link[=vl_gpar]{vl_gpar()}}.}

\item{name}{Optional name (for \code{\link[=edit_node]{edit_node()}}).}

\item{vp}{Optional \code{\link[=vl_viewport]{vl_viewport()}} to draw this grob inside.}

\item{id}{For most grobs, an optional semantic identifier emitted by the SVG
backend as \code{data-vellum-id} (for interactivity, accessibility, and testing;
ignored by raster/PDF). \strong{For \code{path_grob} only}, \code{id} instead groups points
(one value per point) into closed sub-paths: all points sharing an \code{id} form
one sub-path (so a hole is a separate \code{id}), in first-appearance order (à la
grid); \code{NULL} makes a single sub-path.}

\item{role}{Optional ARIA role, emitted by the SVG backend as \verb{role=} for
accessibility (ignored by the raster and PDF backends).}
}
\value{
A grob.
}
\description{
\code{text_path_grob()} places a label along a polyline baseline instead of at a
point: each glyph keeps the pen position shaping gave it and is drawn at that
distance along the path, rotated to the local tangent. Use it for labels that
follow an arc, a contour, a river or a curved axis.
}
\details{
The path is a sequence of points, exactly like \code{\link[=lines_grob]{lines_grob()}} — pass the
output of \code{\link[=bezier_grob]{bezier_grob()}}-style control points already flattened, or any
polyline. Arc length is measured on the \emph{rendered} path, so the result adapts
to the viewport the grob is drawn in.

\code{just} does double duty: the horizontal component slides the run along the
baseline (\code{"left"} starts at the first point, \code{"centre"} centres it,
\code{"right"} ends at the last), and the vertical component positions the glyphs
against the baseline as it would for ordinary text. For finer control of the
standoff — a label riding just above a curve rather than sitting on it — use
\code{offset}.

Glyphs follow the tangent, as in SVG \code{textPath}: a label on the underside
of a closed curve reads upside-down. That is the honest result of the
geometry, and the fix is to reverse the path rather than the glyphs — set the
lower arc as a second \code{text_path_grob()} whose points run the other way.

Glyphs are placed one per character. For Latin text that is exact; scripts
where shaping produces a different number of glyphs than characters (Arabic,
Devanagari, and ligature-heavy fonts) will still draw, but the per-glyph
text recovered by PDF copy-paste and by SVG native text degrades to the whole
label on the first glyph.
}
\examples{
th <- seq(pi, 0, length.out = 60)
vl_scene(4, 2.2, dpi = 96, bg = "white") |>
  draw(text_path_grob(
    "text that follows a curve",
    x = 0.5 + 0.42 * cos(th), y = 0.15 + 0.7 * sin(th),
    offset = 3, gp = vl_gpar(fontsize = 13)
  ))
}
