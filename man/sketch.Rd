% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grob.R
\name{sketch}
\alias{sketch}
\title{Hand-drawn ("sketch") rendering}
\usage{
sketch(
  roughness = 1,
  bowing = 1,
  fill_style = c("hachure", "solid", "crosshatch", "zigzag", "dots"),
  fill_weight = NULL,
  hachure_angle = -41,
  hachure_gap = NULL,
  curve_tightness = 0,
  disable_multi_stroke = FALSE,
  preserve_vertices = FALSE,
  seed = 1L
)
}
\arguments{
\item{roughness}{Wobble amount (\verb{>= 0}; \code{0} is nearly crisp, \code{1} the default
hand-drawn look, higher is wilder).}

\item{bowing}{How much straight edges bow (0 disables bowing).}

\item{fill_style}{One of \code{"hachure"} (default), \code{"solid"}, \code{"crosshatch"},
\code{"zigzag"}, \code{"dots"}. Non-solid styles paint the fill colour as line work.}

\item{fill_weight}{Stroke width of fill/hachure lines, in \code{lwd} units
(1 == 1/96 inch); \code{NULL} derives it from the grob's \code{lwd}.}

\item{hachure_angle}{Hachure line angle in degrees.}

\item{hachure_gap}{Gap between hachure lines, in \code{lwd} units; \code{NULL} = auto.}

\item{curve_tightness}{Curve fit tightness for round shapes (circles, arcs).}

\item{disable_multi_stroke}{If \code{TRUE}, draw single (not doubled) outline
strokes — a cleaner, less sketchy line.}

\item{preserve_vertices}{If \code{TRUE}, keep shape vertices exact (only edges wobble).}

\item{seed}{Integer seed for the wobble (same seed => identical output).}
}
\value{
A \code{vellum_sketch} object for a grob's \code{sketch} argument.
}
\description{
Attach to a grob's \code{sketch} argument to render it in a hand-drawn, sketchy
style (the \href{https://roughjs.com}{Rough.js} look): wobbly outlines and hachure
fills. Supported by \code{\link[=rect_grob]{rect_grob()}}, \code{\link[=polygon_grob]{polygon_grob()}}, \code{\link[=lines_grob]{lines_grob()}}, and
\code{\link[=circle_grob]{circle_grob()}}. Output is deterministic given \code{seed}.
}
\details{
Sketch is a deliberate exception to vellum's crisp, fidelity-first defaults —
see \code{vignette} / \verb{_docs/DESIGN-ROUGHR.md}. Text is never sketched.
}
\examples{
rect_grob(gp = gpar(fill = "steelblue", col = "black"), sketch = sketch())
}
