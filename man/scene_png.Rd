% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{scene_png}
\alias{scene_png}
\alias{scene_pdf}
\title{Render a scene to PNG or PDF bytes}
\usage{
scene_png(scene, scale = 1)

scene_pdf(scene)
}
\arguments{
\item{scene}{A \code{\link[=vl_scene]{vl_scene()}}, or anything with an \code{\link[=as_vellum_scene]{as_vellum_scene()}} method.}

\item{scale}{Resolution multiplier (default \code{1}). \code{scale = 2} renders at twice
the device pixels while keeping the \strong{same physical size} — the retina /
\code{ggsave(scaling=)} idiom. It multiplies \code{dpi}, so absolute units (\code{mm},
\code{pt}, \verb{in}) cover proportionally more pixels and nothing about the layout
changes; text does not get relatively bigger or smaller. Only raster output
gains anything: a PDF's page size in points and an SVG's physical size are
unchanged by construction.}
}
\value{
A raw vector: the encoded PNG or PDF document.
}
\description{
Like \code{\link[=render]{render()}} with a \code{.png} / \code{.pdf} path, but returns the encoded document
as a raw vector instead of writing a file. These are the in-memory entry
points for a host that needs the bytes rather than a path — embedding a
base64 data URI in an HTML widget, serving a plot from a web API, or writing
to a connection — without a temp-file round-trip.
}
\details{
Together with \code{\link[=scene_svg]{scene_svg()}} (a string) and \code{\link[=scene_raster]{scene_raster()}} (pixels), every
output format vellum supports can now be produced without touching disk.

Backend degradation warnings (see \code{\link[=render]{render()}}) are surfaced here too, so a PDF
that could not honour a pattern or mask reports it exactly as writing a file
would.
}
\examples{
s <- vl_scene(2, 2) |> draw(circle_grob(gp = vl_gpar(fill = "steelblue")))
png_bytes <- scene_png(s)
length(png_bytes)
rawToChar(png_bytes[2:4]) # "PNG"
}
\seealso{
\code{\link[=render]{render()}}, \code{\link[=scene_svg]{scene_svg()}}, \code{\link[=scene_raster]{scene_raster()}}
}
