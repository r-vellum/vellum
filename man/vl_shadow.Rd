% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paint.R
\name{vl_shadow}
\alias{vl_shadow}
\title{Drop shadow for a viewport group}
\usage{
vl_shadow(dx = 2, dy = 2, blur = 3, col = "#00000059")
}
\arguments{
\item{dx, dy}{Offset in points; positive \code{dy} moves the shadow down.}

\item{blur}{Blur radius in points. \code{0} gives a hard offset silhouette.}

\item{col}{Shadow colour; usually a translucent black.}
}
\value{
A \code{vellum_shadow} object, for \code{vl_viewport(shadow = )}.
}
\description{
Describes a shadow cast by everything drawn inside a \code{\link[=vl_viewport]{vl_viewport()}}: the
group's own silhouette, tinted, blurred and offset, painted underneath it.
Because it is a \emph{group} effect, overlapping shapes inside the viewport cast
one shadow together rather than each casting its own onto the others.
}
\examples{
vl_scene(3, 2) |>
  push(vl_viewport(width = 0.6, height = 0.6, shadow = vl_shadow())) |>
  draw(circle_grob(gp = vl_gpar(fill = "steelblue", col = NA))) |>
  pop()
}
\seealso{
\code{\link[=vl_viewport]{vl_viewport()}}
}
