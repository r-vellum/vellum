% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/device.R
\name{as_vellum}
\alias{as_vellum}
\alias{render_grid}
\title{Render grid graphics (ggplot2 / lattice / grid) through vellum}
\usage{
as_vellum(x, width = 7, height = 7, dpi = 96, bg = "white")

render_grid(
  x,
  path,
  width = 7,
  height = 7,
  dpi = 96,
  bg = "white",
  text = c("native", "outline")
)
}
\arguments{
\item{x}{A grid grob/gTree/gtable, or a ggplot object.}

\item{width, height}{Page size in inches.}

\item{dpi, bg}{As in \code{\link[=vl_scene]{vl_scene()}}.}

\item{path}{Output file (\code{.png}/\code{.svg}/\code{.pdf}).}

\item{text}{Passed to \code{\link[=render]{render()}} (SVG text mode).}
}
\value{
\code{as_vellum()}: a \code{vellum_scene}. \code{render_grid()}: \code{path}, invisibly.
}
\description{
\code{as_vellum()} converts a grid grob tree — or a ggplot2 plot — into a
\code{\link[=vl_scene]{vl_scene()}} by letting an offscreen grid device resolve all coordinates to
absolute units, then emitting vellum grobs. \code{render_grid()} does that and
writes the result. This is the interop path (grid-based graphics rendered by
vellum's deterministic backend); a native graphics device is future work.
}
\examples{
\dontrun{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg)) + geom_point()
render_grid(p, "plot.png", width = 6, height = 4)
}
}
