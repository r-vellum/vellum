% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geometry2.R
\name{vl_path_op}
\alias{vl_path_op}
\title{Boolean operations on paths}
\usage{
vl_path_op(
  a,
  b,
  op = c("union", "intersect", "difference", "xor"),
  rule = c("nonzero", "evenodd"),
  gp = vl_gpar(),
  name = NULL,
  vp = NULL,
  role = NULL
)
}
\arguments{
\item{a, b}{The operands: a \code{\link[=path_grob]{path_grob()}}, a \code{\link[=polygon_grob]{polygon_grob()}}, or a list with
\code{x}, \code{y} and optionally \code{nper} (points per ring).}

\item{op}{One of \code{"union"}, \code{"intersect"}, \code{"difference"} (\code{a} minus \code{b}), or
\code{"xor"}.}

\item{rule}{Fill rule for interpreting the inputs: \code{"nonzero"} (default) or
\code{"evenodd"}.}

\item{gp, name, vp, role}{Passed to the returned \code{\link[=path_grob]{path_grob()}}.}
}
\value{
A \code{\link[=path_grob]{path_grob()}} with \code{rule = "winding"}. An empty result (a
disjoint intersection, say) gives a grob with no points, which draws
nothing.
}
\description{
Combine two shapes as \strong{geometry}: union, intersection, difference or
exclusive-or over their closed rings.
}
\section{Why geometry and not a mask}{

vellum can already clip one shape by another at render time, and for simply
\emph{showing} an intersection that is often enough. A boolean result is different
in kind: it is an ordinary path, so it can be measured, hit-tested,
simplified, filled with a gradient, stroked, exported as \verb{<path>} data, and
fed into another boolean. A mask can do none of those, rasterises, and
degrades on some PDF paths.
}

\section{Rings, holes and the fill rule}{

\code{rule} says how to interpret the \strong{inputs} — whether a ring inside another
ring is a hole (\code{"evenodd"}) or a separate island (\code{"nonzero"}). It must match
the rule the operands were drawn with, or the answer will be correct for a
shape you did not mean.

The \strong{result} always uses the non-zero rule: holes come back wound opposite
to their outer ring, which is how the returned \code{path_grob()} is set up.

Operands must be in a single coordinate space — one unit, no offsets — since
a boolean has to be computed somewhere, and mixed units have no common space.
}

\examples{
sq <- function(x0, y0, s = 0.4) {
  list(x = c(x0, x0 + s, x0 + s, x0), y = c(y0, y0, y0 + s, y0 + s))
}
a <- sq(0.2, 0.3)
b <- sq(0.45, 0.4)
vl_scene(3, 2, dpi = 96, bg = "white") |>
  draw(vl_path_op(a, b, "union", gp = vl_gpar(fill = "#DCE7F5", col = "steelblue")))
}
