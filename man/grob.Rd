% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grob.R
\name{grob}
\alias{grob}
\alias{rect_grob}
\alias{roundrect_grob}
\alias{lines_grob}
\alias{polygon_grob}
\alias{bezier_grob}
\alias{spline_grob}
\alias{circle_grob}
\alias{points_grob}
\alias{hexagon_grob}
\alias{sector_grob}
\alias{loop_grob}
\alias{segments_grob}
\alias{path_grob}
\alias{raster_grob}
\alias{text_grob}
\title{Graphical objects (grobs)}
\usage{
rect_grob(
  x = 0.5,
  y = 0.5,
  width = 1,
  height = 1,
  gp = gpar(),
  name = NULL,
  vp = NULL,
  id = NULL,
  role = NULL
)

roundrect_grob(
  x = 0.5,
  y = 0.5,
  width = 1,
  height = 1,
  r = 0.1,
  gp = gpar(),
  name = NULL,
  vp = NULL,
  id = NULL,
  role = NULL
)

lines_grob(
  x,
  y,
  arrow = NULL,
  start_cap = NULL,
  end_cap = NULL,
  gp = gpar(),
  name = NULL,
  vp = NULL,
  id = NULL,
  role = NULL
)

polygon_grob(x, y, gp = gpar(), name = NULL, vp = NULL, id = NULL, role = NULL)

bezier_grob(
  x,
  y,
  n = 60,
  gp = gpar(),
  name = NULL,
  vp = NULL,
  id = NULL,
  role = NULL
)

spline_grob(
  x,
  y,
  shape = 1,
  n = 20,
  open = TRUE,
  gp = gpar(),
  name = NULL,
  vp = NULL,
  id = NULL,
  role = NULL
)

circle_grob(
  x = 0.5,
  y = 0.5,
  r = 0.25,
  gp = gpar(),
  name = NULL,
  vp = NULL,
  id = NULL,
  role = NULL
)

points_grob(
  x,
  y,
  size = unit(2, "mm"),
  shape = "circle",
  gp = gpar(),
  name = NULL,
  vp = NULL,
  id = NULL,
  role = NULL
)

hexagon_grob(
  x = 0.5,
  y = 0.5,
  size = unit(2, "mm"),
  width = NULL,
  height = NULL,
  fill = NULL,
  orientation = c("flat", "pointy"),
  gp = gpar(),
  name = NULL,
  vp = NULL,
  id = NULL,
  role = NULL
)

sector_grob(
  x = 0.5,
  y = 0.5,
  r0 = 0,
  r1 = 0.5,
  theta0 = 0,
  theta1 = 2 * pi,
  fill = NULL,
  arrow = NULL,
  gp = gpar(),
  name = NULL,
  vp = NULL,
  id = NULL,
  role = NULL
)

loop_grob(
  x = 0.5,
  y = 0.5,
  r = unit(2, "mm"),
  theta0 = 0,
  theta1 = 1.5 * pi,
  arrow = NULL,
  gp = gpar(),
  name = NULL,
  vp = NULL,
  id = NULL,
  role = NULL
)

segments_grob(
  x0,
  y0,
  x1,
  y1,
  arrow = NULL,
  start_cap = NULL,
  end_cap = NULL,
  gp = gpar(),
  name = NULL,
  vp = NULL,
  id = NULL,
  role = NULL
)

path_grob(
  x,
  y,
  id = NULL,
  rule = c("winding", "evenodd"),
  gp = gpar(),
  name = NULL,
  vp = NULL,
  role = NULL
)

raster_grob(
  image,
  x = 0.5,
  y = 0.5,
  width = 1,
  height = 1,
  interpolate = TRUE,
  gp = gpar(),
  name = NULL,
  vp = NULL,
  id = NULL,
  role = NULL
)

text_grob(
  label,
  x = 0.5,
  y = 0.5,
  just = "centre",
  rot = 0,
  gp = gpar(),
  name = NULL,
  vp = NULL,
  id = NULL,
  role = NULL
)
}
\arguments{
\item{x, y}{Coordinates (\code{\link[=unit]{unit()}} or numeric).}

\item{width, height}{Optional per-hexagon \strong{full} extent (corner-to-corner) along
the x and y axis, as \code{\link[=unit]{unit()}}s recycled like \code{x}/\code{y}. When both are supplied
they override \code{size}, resolved per-axis, so a hexagon can be \emph{non-regular}
(independent horizontal and vertical extent) and tile a non-square lattice —
e.g. \code{width}/\code{height} in \code{"native"} units tile in data space regardless of the
device aspect. \code{width} is the distance between the left and right vertices (for
\code{"flat"}; the flat sides for \code{"pointy"}) and \code{height} the distance between the
top and bottom edges (\code{"flat"}; vertices for \code{"pointy"}). A \emph{regular} hexagon
is \code{height == width * sqrt(3) / 2} (flat). Leave both \code{NULL} (default) to draw
a regular hexagon of circumradius \code{size}. Must be given together.}

\item{gp}{Graphical parameters, from \code{\link[=gpar]{gpar()}}.}

\item{name}{Optional name (for \code{\link[=edit_node]{edit_node()}}).}

\item{vp}{Optional \code{\link[=viewport]{viewport()}} to draw this grob inside.}

\item{id}{For most grobs, an optional semantic identifier emitted by the SVG
backend as \code{data-vellum-id} (for interactivity, accessibility, and testing;
ignored by raster/PDF). \strong{For \code{path_grob} only}, \code{id} instead groups points
(one value per point) into closed sub-paths: all points sharing an \code{id} form
one sub-path (so a hole is a separate \code{id}), in first-appearance order (à la
grid); \code{NULL} makes a single sub-path.}

\item{role}{Optional ARIA role, emitted by the SVG backend as \verb{role=} for
accessibility (ignored by the raster and PDF backends).}

\item{r}{Loop radius: an \strong{absolute} \code{\link[=unit]{unit()}} (\code{mm}/\code{cm}/\verb{in}/\code{pt}; a bare
numeric is \code{mm}), resolved to a device length \strong{at render} so it tracks a
node's size at any page size/dpi. Recycled per loop.}

\item{arrow}{An \code{\link[=arrow]{arrow()}} spec to draw heads on the line/segment ends, or
\code{NULL} for none.}

\item{start_cap, end_cap}{Optional \strong{absolute-length} \code{\link[=unit]{unit()}}s (\code{mm}/\code{cm}/
\verb{in}/\code{pt}; a bare numeric is taken as \code{mm}) that shorten the drawn line
inward from its start/end by that physical amount, resolved \strong{at render} in
device space — so the gap is exact at any size, dpi, and aspect ratio, with
no reliance on the native scale. For \code{\link[=segments_grob]{segments_grob()}} the caps are
per-element (scalar or length-n, recycled like the coordinates); for
\code{\link[=lines_grob]{lines_grob()}} a single (scalar) cap trims each end of the whole polyline.
\code{NULL} (default) leaves the endpoint untouched. When an \code{\link[=arrow]{arrow()}} is also
present its head is placed at the \emph{capped} end, so the tip lands on the
boundary (e.g. a node marker) rather than under it. This is what lets a
directed edge stop at a node's radius. See the acceptance notes in the
package for the degenerate cases (a cap \code{>=} the segment length draws
nothing; a zero-length segment is skipped).}

\item{n}{Number of points to sample the curve at (flattened to a polyline).}

\item{shape}{Marker shape(s): \code{"circle"} (default), \code{"square"}, \code{"triangle"},
\code{"diamond"}, \code{"plus"}, or \code{"cross"}, recycled per point. Filled shapes use
\code{gp$fill} (and outline \code{gp$col}); \code{"plus"}/\code{"cross"} are stroke-only.}

\item{open}{If \code{FALSE}, the spline is closed (wraps end to start).}

\item{size}{Point size (\code{\link[=unit]{unit()}} or numeric).}

\item{fill}{Per-element fill colour(s), recycled to the number of sectors. \code{NULL}
falls back to \code{gp$fill}.}

\item{orientation}{Hexagon orientation: \code{"flat"} (default, flat top/bottom edge)
or \code{"pointy"} (vertex at top). \code{size} is the circumradius (centre to vertex).}

\item{r0, r1}{Inner and outer radius of each sector (\code{\link[=unit]{unit()}} or numeric;
numeric is treated as \code{"native"}). \code{r0 = 0} gives a pie slice; \code{r0 == r1}
gives an arc outline (stroke only, no fill).}

\item{theta0, theta1}{Start and end angle of each sector, in \strong{radians}, with 0
at 3 o'clock and increasing counter-clockwise.}

\item{x0, y0, x1, y1}{Segment start/end coordinates (\code{\link[=unit]{unit()}} or numeric).}

\item{rule}{Fill rule: \code{"winding"} (non-zero, default) or \code{"evenodd"}.}

\item{image}{A raster image: a \code{\link[grDevices:as.raster]{grDevices::as.raster()}}-compatible object — a
matrix/array of colours or greyscale values, or a \code{raster} object.}

\item{interpolate}{Smoothly interpolate when scaling (default \code{TRUE})? \code{FALSE}
keeps hard pixel edges.}

\item{label}{Character string(s) to draw.}

\item{just}{Justification: \code{c(hjust, vjust)} as names (\code{"left"}, \code{"centre"},
\code{"right"}, \code{"bottom"}, \code{"top"}) or numbers in \verb{[0, 1]}.}

\item{rot}{Rotation in degrees, counter-clockwise.}
}
\value{
A grob object.
}
\description{
Grobs are immutable value objects describing something to draw. Build them
with the constructors below, add them to a scene with \code{\link[=draw]{draw()}}, and render
with \code{\link[=render]{render()}}. Coordinates accept a \code{\link[=unit]{unit()}} vector or a bare numeric
(interpreted in the \code{default_units}, usually \code{"npc"}).
}
\details{
\code{sector_grob()} draws a batch of annular sectors (pie / donut / rose wedges) in a
single call. \code{gp$fill} recycles per sector; \code{gp$col}/\code{lwd} give a uniform stroke.

Passing \code{r0 == r1} gives an \strong{open arc} (stroke only). Combined with an
absolute (\code{mm}) radius at a \code{"native"} centre and an \code{\link[=arrow]{arrow()}}, this draws a
resolution-independent directed \strong{self-loop}: the radius is resolved to a
device length at render (like a marker \code{size}), so it tracks a node's mm radius
at any page size or dpi. The arrowhead sits tangent to the outer arc's end (see
\code{\link[=loop_grob]{loop_grob()}} for the convenience wrapper).

\code{loop_grob()} draws directed/undirected \strong{self-loops} for node-link diagrams: an
open arc centred at a \code{"native"} anchor \verb{(x, y)} with an absolute (mm) radius \code{r},
from \code{theta0} to \code{theta1} (radians), optionally with an \code{\link[=arrow]{arrow()}} tangent to its
end. It is a thin wrapper over \code{\link[=sector_grob]{sector_grob()}} with \code{r0 = r1 = r}. Because \code{r} is
absolute and deferred to render, the loop stays proportional to the node's mm
radius under any device size — no native-per-mm estimation.
}
