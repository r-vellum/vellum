% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spread.R
\name{spread}
\alias{spread}
\title{Spread (dilate) the pixels of a raster grob}
\usage{
spread(grob, px = 1L, shape = c("circle", "square"), how = c("over", "add"))
}
\arguments{
\item{grob}{A raster \link{grob} (e.g. from \code{\link[=datashade]{datashade()}}). Other grob kinds
are returned unchanged.}

\item{px}{Spread radius in pixels (non-negative integer). \code{0} returns \code{grob}
unchanged.}

\item{shape}{Neighbourhood shape: \code{"circle"} (default, Euclidean radius) or
\code{"square"} (Chebyshev radius).}

\item{how}{How overlapping spread pixels combine: \code{"over"} (default) keeps the
colour of the most-opaque contributor; \code{"add"} accumulates opacity (clamped)
so overlaps darken.}
}
\value{
A raster \link{grob} with the spread applied.
}
\description{
Grow each non-empty pixel of a raster over a small neighbourhood so thin marks
stay visible. This is \href{https://datashader.org}{datashader's} \code{spread}: useful
after \code{\link[=datashade]{datashade()}}, \code{\link[=datashade_lines]{datashade_lines()}}, or \code{\link[=datashade_segments]{datashade_segments()}} when the
output is sparse (single-pixel lines, isolated points). \code{\link[=dynspread]{dynspread()}} chooses
the radius automatically from the image's density; \code{spread()} uses a fixed one.
}
\examples{
set.seed(1)
g <- datashade_segments(rnorm(2000), rnorm(2000), rnorm(2000), rnorm(2000))
g2 <- spread(g, px = 2)
}
\seealso{
\code{\link[=dynspread]{dynspread()}}, \code{\link[=datashade]{datashade()}}, \code{\link[=datashade_lines]{datashade_lines()}}.
}
