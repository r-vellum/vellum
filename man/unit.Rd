% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unit.R
\name{unit}
\alias{unit}
\alias{is_unit}
\title{Units of measurement}
\usage{
unit(values, units = "npc", data = NULL)

is_unit(x)
}
\arguments{
\item{values}{Numeric vector of magnitudes.}

\item{units}{Character vector of unit names, recycled against \code{values}.}

\item{data}{Optional list supplying context for derived units:
\code{label}, \code{fontfamily}, \code{fontface}, \code{fontsize}, \code{lineheight}.}

\item{x}{An object.}
}
\value{
A \code{unit} vector.
}
\description{
A \code{unit} is a vectorised \verb{(value, unit)} pair used for coordinates and sizes.
Each element carries its own unit, so a single \code{unit} vector can mix
coordinate systems and a primitive can use different units on the x and y
axes (e.g. \code{x} in \code{"native"}, \code{y} in \code{"npc"}).
}
\details{
Supported units:
\itemize{
\item \code{"npc"} — normalised parent coordinates (0 = bottom/left, 1 = top/right)
\item \code{"native"} — the enclosing viewport's \code{xscale}/\code{yscale}
\item \code{"mm"}, \code{"cm"}, \code{"in"}, \code{"pt"} — absolute lengths
\item \code{"char"}, \code{"line"} — font-relative (need \code{fontsize}/\code{lineheight} via \code{data})
\item \code{"strwidth"}, \code{"strheight"} — size of a string (need \code{label} via \code{data})
}

Font- and string-relative units are resolved to absolute millimetres at
construction (text metrics are available device-independently), so a stored
\code{unit} only ever holds one of the core backend units.

Arithmetic: \code{+} and \code{-} combine two units of the \emph{same} code, or two
\emph{absolute} units (\code{"mm"}/\code{"cm"}/\code{"in"}/\code{"pt"}), which resolve to \code{"mm"}
immediately (e.g. \code{unit(10, "mm") + unit(1, "in")} is \verb{35.4mm}).
\code{unit * scalar} scales. Mixing a normalised/native unit with an absolute one
(e.g. \code{unit(1, "npc") - unit(2, "mm")}) is genuinely deferred and errors —
compose such offsets at the viewport/native level or pre-resolve to \code{"mm"}.
}
\examples{
unit(1:3, "native")
unit(c(0.5, 1), c("npc", "in"))
}
