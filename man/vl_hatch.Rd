% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paint.R
\name{vl_hatch}
\alias{vl_hatch}
\title{Hatch fill}
\usage{
vl_hatch(angle = 45, spacing = 3, width = 0.75, col = "black", bg = NA)
}
\arguments{
\item{angle}{Direction of the rules, in degrees counter-clockwise from
horizontal. Distinct angles (0, 45, 90, 135) read as distinct categories.}

\item{spacing}{Distance between rules, in points.}

\item{width}{Rule width, in points.}

\item{col}{Rule colour.}

\item{bg}{Optional background painted behind the rules. \code{NA} (default) leaves
whatever is underneath showing through.}
}
\value{
A \code{vellum_hatch} object, usable anywhere a \code{fill} is.
}
\description{
Fills a shape with ruled parallel lines. Unlike \code{\link[=vl_pattern]{vl_pattern()}}, which
rasterises a tile, a hatch is \strong{geometry}: it stays crisp at any zoom, prints
correctly, and survives being converted to greyscale.
}
\details{
That last point is the reason to reach for it. A colour encoding that fails
for a red/green-blind reader — which \code{\link[=render]{render()}}'s \code{cvd} argument will show you
and \code{\link[=vl_lint]{vl_lint()}} will flag — is fixed by encoding with \emph{texture} as well as
hue. Hatching is the standard way to do that.
}
\examples{
vl_scene(3, 2) |>
  draw(rect_grob(width = 0.8, height = 0.8, gp = vl_gpar(
    fill = vl_hatch(angle = 45, spacing = 4), col = "grey30"
  )))
}
\seealso{
\code{\link[=vl_pattern]{vl_pattern()}} for a tiled grob, \code{\link[=linear_gradient]{linear_gradient()}}, \code{\link[=vl_lint]{vl_lint()}}
}
