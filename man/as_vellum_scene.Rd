% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{as_vellum_scene}
\alias{as_vellum_scene}
\title{Coerce an object to a vellum scene}
\usage{
as_vellum_scene(x, ...)
}
\arguments{
\item{x}{An object to coerce: a \code{vellum_scene}, or a type a downstream package
has taught to compile by defining an \code{as_vellum_scene()} method.}

\item{...}{Passed on to methods.}
}
\value{
A \code{vellum_scene}.
}
\description{
The extensible seam a higher-level package (e.g. a grammar layer) implements to
compile its own plot object into a \code{\link[=vl_scene]{vl_scene()}}. \code{\link[=render]{render()}} coerces its input
through this generic, so \code{render(x, path)} works for any \code{x} that has an
\code{as_vellum_scene()} method. An identity method for \code{vellum_scene} is provided.
}
\details{
This is the stable \emph{compiler-backend} entry point: downstream packages should
target \code{as_vellum_scene()} (and the exported grob/viewport/unit constructors)
rather than vellum's internal \code{compile()} / \code{.scene_to_backend()} helpers.
}
\examples{
sc <- vl_scene()
identical(as_vellum_scene(sc), sc) # the identity method returns its input
}
