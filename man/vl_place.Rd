% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/place.R
\name{vl_place}
\alias{vl_place}
\alias{vl_repel}
\title{Move labels so they stop overlapping}
\usage{
vl_place(
  scene,
  labels = NULL,
  avoid = NULL,
  padding = 1,
  max_shift = 10,
  max_iter = 200,
  pull = 0.1
)

vl_repel(
  scene,
  labels = NULL,
  avoid = NULL,
  padding = 1,
  max_shift = 10,
  max_iter = 200,
  pull = 0.1
)
}
\arguments{
\item{scene}{A \code{\link[=vl_scene]{vl_scene()}} (or anything with an \code{\link[=as_vellum_scene]{as_vellum_scene()}} method).}

\item{labels}{Names of the grobs to move. \code{NULL} (default) takes every text
node in the scene.}

\item{avoid}{Names of the grobs to treat as obstacles. \code{NULL} (default) takes
every node that is not a label.}

\item{padding}{Clear space to keep around each label, in millimetres.}

\item{max_shift}{The furthest a label may travel from its anchor, in
millimetres. Labels needing more are left at their best position and
reported as unresolved.}

\item{max_iter}{Iteration cap for the relaxation.}

\item{pull}{Strength of the spring back to the anchor, in \verb{[0, 1]}. Higher
keeps labels closer to what they label; lower resolves crowding more
readily.}
}
\value{
\code{vl_place()}: a data frame with one row per label — \code{name}, \code{index}
(which element of that grob), the original device box \code{x0}/\code{y0}/\code{x1}/\code{y1},
the shift \code{dx}/\code{dy} in millimetres, and \code{resolved} (did it end up clear).
\code{vl_repel()}: a new scene.
}
\description{
\code{vl_place()} solves label collisions over a scene's \strong{resolved} geometry and
reports the displacement each label needs. \code{vl_repel()} applies that solution
and returns a new scene.
}
\section{Why this is an engine service}{

Repulsion is a geometry problem over boxes: where things ended up, how big
they are, and what they must not touch. It carries no data semantics, so
every layer above vellum would otherwise reimplement it — and reimplement it
against numbers it can only get by rendering first.

Because the solve happens in \strong{device pixels} and the answer is applied as an
absolute millimetre offset on top of each label's existing coordinate, it does
not care what produced the position. A label anchored in \code{native} units inside
a polar viewport, a facet panel, or a warped coordinate system moves by the
same mechanism as one in \code{npc} on the page, and panels are all solved together
rather than one at a time.
}

\section{What it does not do}{

It moves labels; it does not choose which to drop, shrink or abbreviate —
those are editorial decisions belonging to the layer that knows what the
labels mean. Labels that cannot be separated within \code{max_shift} are reported
with \code{resolved = FALSE} rather than silently piled up or quietly deleted.
}

\examples{
set.seed(1)
n <- 12
s <- vl_scene(5, 3, dpi = 96, bg = "white") |>
  draw(points_grob(runif(n), runif(n), gp = vl_gpar(fill = "steelblue", col = NA),
                   name = "pts")) |>
  draw(text_grob(paste0("item ", seq_len(n)), x = runif(n), y = runif(n),
                 gp = vl_gpar(fontsize = 9), name = "lab"))
head(vl_place(s))
}
\seealso{
\code{\link[=vl_empty_region]{vl_empty_region()}} for placing one thing in the gap rather than
moving many things apart.
}
