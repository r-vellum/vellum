% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serialize.R
\name{as_scene_spec}
\alias{as_scene_spec}
\alias{from_scene_spec}
\title{Convert a scene to and from a plain list}
\usage{
as_scene_spec(scene)

from_scene_spec(spec)
}
\arguments{
\item{scene}{A \code{\link[=vl_scene]{vl_scene()}}, or anything with an \code{\link[=as_vellum_scene]{as_vellum_scene()}} method.}

\item{spec}{A list from \code{as_scene_spec()}.}
}
\value{
\code{as_scene_spec()}: a nested list, with a \code{version} element.
\code{from_scene_spec()}: a \code{vellum_scene}.
}
\description{
\code{as_scene_spec()} turns a rendered-ready scene into a plain nested list of R
vectors, and \code{from_scene_spec()} rebuilds the scene from it. Together they
make a compiled scene a \emph{value you can store or send}, rather than something
that only exists inside the session that built it.
}
\details{
This is one level below \code{vellumplot}'s plot spec. A plot spec is portable and
re-renderable at any size; a scene spec is the \emph{resolved} artifact — the grobs,
units and viewports as they will actually be drawn. They compose: keep the
plot spec to re-render, keep the scene spec to reproduce exactly this scene.

The conversion is generic over the S7 property model rather than written per
grob type, so a grob added to vellum later serializes with no change here.
}
\examples{
s <- vl_scene(3, 2) |>
  draw(circle_grob(r = 0.3, gp = vl_gpar(fill = "tomato", col = NA)))
spec <- as_scene_spec(s)
spec$version
identical(as_scene_spec(from_scene_spec(spec)), spec)
}
\seealso{
\code{\link[=scene_write]{scene_write()}}, \code{\link[=scene_diff]{scene_diff()}}, \code{\link[=scene_hash]{scene_hash()}}
}
