% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fonts.R
\name{scene_fonts}
\alias{scene_fonts}
\title{Which fonts a scene actually used}
\usage{
scene_fonts(scene)
}
\arguments{
\item{scene}{A \code{\link[=vl_scene]{vl_scene()}} (or anything with an \code{\link[=as_vellum_scene]{as_vellum_scene()}} method).}
}
\value{
A data frame with \code{path}, \code{index} (the face within a font collection),
\code{glyphs} (how many glyphs came from that face), \code{file} (the basename) and
\code{exists}. Zero rows if the scene has no text.
}
\description{
Reports the font \strong{files} a scene's text resolved to, read off the shaped
glyphs rather than re-resolved from family names — so it says what was used,
not what would be picked if asked again.
}
\examples{
s <- vl_scene(4, 2) |> draw(text_grob("hello", gp = vl_gpar(fontfamily = "serif")))
scene_fonts(s)
}
\seealso{
\code{\link[=font_pin]{font_pin()}} to record this and check it later.
}
