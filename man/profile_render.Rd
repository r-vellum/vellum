% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{profile_render}
\alias{profile_render}
\title{Where a scene spends its render time}
\usage{
profile_render(scene, reps = 3)
}
\arguments{
\item{scene}{A \code{\link[=vl_scene]{vl_scene()}}, or anything with an \code{\link[=as_vellum_scene]{as_vellum_scene()}} method.}

\item{reps}{Number of render repetitions to time; the median is reported.}
}
\value{
A data frame ordered by cost, with columns \code{kind}, \code{name}, \code{n}
(elements), \code{seconds} and \code{pct} (share of raster time). Rows are aggregated
per mark — a vectorised \code{text_grob()} of 200 labels compiles to 200 nodes
but reports as one row — so the table reads as "which of my marks is
expensive". The phase split is attached as the \code{"phases"} attribute and
shown when printed.
}
\description{
Attributes render cost to the individual marks that caused it, and splits the
three phases a render goes through: \strong{build} (constructing the R value),
\strong{compile} (the R→Rust replay, including text shaping), and \strong{raster}
(drawing). Turns "this plot is slow" into a specific answer.
}
\details{
Read the phase split first. Compile time is R-side — S7 property validation,
grob construction, unit records — and on scenes with many small grobs it
routinely dominates, in which case no amount of backend tuning will help. The
per-node table only accounts for raster time.

Timing is armed only for this call, so ordinary renders pay nothing for it.
}
\examples{
set.seed(1)
s <- vl_scene(4, 3) |>
  draw(points_grob(runif(2000), runif(2000))) |>
  draw(text_grob("title", y = 0.95, gp = vl_gpar(fontsize = 16)))
profile_render(s)
}
\seealso{
\code{\link[=vl_lint]{vl_lint()}}, \code{\link[=scene_stats]{scene_stats()}}
}
