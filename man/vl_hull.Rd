% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/place.R
\name{vl_hull}
\alias{vl_hull}
\title{Hull of a point set}
\usage{
vl_hull(x, y, concavity = Inf)
}
\arguments{
\item{x, y}{Point coordinates (plain numerics, in whatever space you are
working in — the hull is scale-free).}

\item{concavity}{Threshold as above; larger is more convex, and \code{Inf} (the
default) is the convex hull.}
}
\value{
A data frame of the hull ring's \code{x}/\code{y}, in order, ready for
\code{\link[=polygon_grob]{polygon_grob()}}. The ring is not closed (the last point does not repeat
the first).
}
\description{
\code{vl_hull()} returns the convex hull, or a concave one that follows the point
set more closely — useful for outlining a cluster, or for building an
exclusion zone that annotations should avoid.
}
\details{
The concave hull digs into the convex hull, replacing an edge with two edges
through the nearest unused point whenever the edge is long relative to the
detour. \code{concavity} is that threshold, and \strong{larger means more convex}:
\itemize{
\item \code{Inf} (the default) is exactly the convex hull.
\item \code{8} follows the points loosely.
\item \code{4} is a good tight outline for a scattered cluster.
\item Below about \code{3} the boundary starts to self-intersect.
}

Self-intersection is inherent to the method, not a defect to be fixed here —
a boundary that threads between interior points has to cross itself
eventually. Inspect the result if you push it hard.
}
\examples{
set.seed(1)
pts <- data.frame(x = runif(40), y = runif(40))
h <- vl_hull(pts$x, pts$y)
vl_scene(3, 3, dpi = 96, bg = "white") |>
  draw(polygon_grob(h$x, h$y, gp = vl_gpar(fill = "#DCE7F5", col = "steelblue"))) |>
  draw(points_grob(pts$x, pts$y, gp = vl_gpar(fill = "grey25", col = NA)))
}
