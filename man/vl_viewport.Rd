% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viewport.R
\name{vl_viewport}
\alias{vl_viewport}
\alias{grid_layout}
\title{Viewports and layouts}
\usage{
vl_viewport(
  x = 0.5,
  y = 0.5,
  width = 1,
  height = 1,
  xscale = c(0, 1),
  yscale = c(0, 1),
  angle = 0,
  clip = FALSE,
  gp = vl_gpar(),
  layout = NULL,
  row = NULL,
  col = NULL,
  rowspan = 1,
  colspan = 1,
  mask = NULL,
  alpha = NULL,
  blend = NULL,
  name = NULL,
  cache = FALSE
)

grid_layout(
  widths = vl_unit(1, "null"),
  heights = vl_unit(1, "null"),
  respect = FALSE
)
}
\arguments{
\item{x, y}{Centre of the viewport (\code{\link[=vl_unit]{vl_unit()}} or numeric, in the parent).}

\item{width, height}{Size (\code{\link[=vl_unit]{vl_unit()}} or numeric, in the parent).}

\item{xscale, yscale}{Length-2 native coordinate ranges.}

\item{angle}{Rotation in degrees, counter-clockwise about the centre.}

\item{clip}{Clip drawing to this viewport: \code{TRUE}/\code{FALSE} for the viewport
rectangle, or a \code{\link[=polygon_grob]{polygon_grob()}}/\code{\link[=path_grob]{path_grob()}} (in this viewport's
coordinates) to clip to an arbitrary path.}

\item{gp}{Inheritable graphical parameters, from \code{\link[=vl_gpar]{vl_gpar()}}.}

\item{layout}{An optional \code{\link[=grid_layout]{grid_layout()}}.}

\item{row, col}{Cell (1-based) of the parent's layout to place into.}

\item{rowspan, colspan}{Number of cells to span.}

\item{mask}{An optional mask: a grob (or list of grobs), or an \code{\link[=as_mask]{as_mask()}}
result. The viewport's contents are rendered as an isolated layer and the
mask modulates their visibility.}

\item{alpha}{Optional group opacity in \verb{[0, 1]}. The viewport's contents are
composited as a single isolated layer at this opacity, so overlapping
elements do not accumulate (unlike per-element \code{vl_gpar(alpha=)}). \code{NULL}
(default) means fully opaque.}

\item{blend}{Optional blend mode for compositing the viewport's contents (as an
isolated layer) onto the backdrop below it. One of \code{"normal"} (default),
\code{"multiply"}, \code{"screen"}, \code{"overlay"}, \code{"darken"}, \code{"lighten"},
\code{"color-dodge"}, \code{"color-burn"}, \code{"hard-light"}, \code{"soft-light"},
\code{"difference"}, \code{"exclusion"}, \code{"hue"}, \code{"saturation"}, \code{"color"}, or
\code{"luminosity"} (the CSS \code{mix-blend-mode} set). \code{NULL}/\code{"normal"} is ordinary
over-compositing.}

\item{name}{Optional name (for \code{\link[=edit_node]{edit_node()}}).}

\item{cache}{Repaint boundary (\code{TRUE}/\code{FALSE}, default \code{FALSE}). Flag this
viewport's subtree as a cached sub-raster: on render it is rasterised once to
its own layer and, on later renders where the subtree is \strong{unchanged}, the
cached pixels are composited instead of re-drawing the subtree. This makes
partial redraw cheap — highlight/hover (\code{\link[=edit_node]{edit_node()}} one element and
re-render) or animation (one subtree changes, others static) re-rasterise
only what changed. Raster/\code{\link[=display]{display()}} only; SVG/PDF ignore it and render the
subtree as vector (no fidelity loss). Ignored when the viewport also sets a
non-normal \code{blend} (a blend needs the live backdrop). See
\code{\link[=vl_clear_render_cache]{vl_clear_render_cache()}}.}

\item{widths, heights}{Track sizes as a \code{\link[=vl_unit]{vl_unit()}} vector. Use \code{"null"} units for
flexible tracks that share leftover space in proportion to their value.}

\item{respect}{Logical; if \code{TRUE}, lock the layout's aspect grid-style: one unit
of \code{"null"} width is forced to the same physical (device) size as one unit of
\code{"null"} height. The axis whose \code{null} unit would be larger shrinks to match
and the whole grid is centered in its parent (so absolute gutter tracks stay
attached to the flexible cells). Encode a desired cell aspect in the \code{null}
track weights — a cell of \code{null} width-weight \code{w} by height-weight \code{h} then
renders with device aspect \code{w:h}. Default \code{FALSE} (tracks just fill the
parent). This is how a fixed-aspect panel (e.g. \code{coord_fixed()}, maps) is
built on top of vellum.}
}
\value{
A \code{viewport} object.

\code{grid_layout()}: a layout object.
}
\description{
A \code{viewport} is a rectangular region that establishes its own coordinate
systems (its \code{xscale}/\code{yscale} for \code{"native"} units), optionally rotated,
clipped, and carrying inheritable graphical parameters. Push one onto a scene
with \code{\link[=push]{push()}}. A viewport may define a row/column \code{\link[=grid_layout]{grid_layout()}}; child
viewports are then placed into cells via \code{row}/\code{col}.
}
\examples{
vl_viewport(xscale = c(0, 10), yscale = c(0, 100))
}
