% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viewport.R
\name{viewport}
\alias{viewport}
\alias{grid_layout}
\title{Viewports and layouts}
\usage{
viewport(
  x = 0.5,
  y = 0.5,
  width = 1,
  height = 1,
  xscale = c(0, 1),
  yscale = c(0, 1),
  angle = 0,
  clip = FALSE,
  gp = gpar(),
  layout = NULL,
  row = NULL,
  col = NULL,
  rowspan = 1,
  colspan = 1,
  mask = NULL,
  alpha = NULL,
  name = NULL
)

grid_layout(widths = unit(1, "null"), heights = unit(1, "null"))
}
\arguments{
\item{x, y}{Centre of the viewport (\code{\link[=unit]{unit()}} or numeric, in the parent).}

\item{width, height}{Size (\code{\link[=unit]{unit()}} or numeric, in the parent).}

\item{xscale, yscale}{Length-2 native coordinate ranges.}

\item{angle}{Rotation in degrees, counter-clockwise about the centre.}

\item{clip}{Clip drawing to this viewport: \code{TRUE}/\code{FALSE} for the viewport
rectangle, or a \code{\link[=polygon_grob]{polygon_grob()}}/\code{\link[=path_grob]{path_grob()}} (in this viewport's
coordinates) to clip to an arbitrary path.}

\item{gp}{Inheritable graphical parameters, from \code{\link[=gpar]{gpar()}}.}

\item{layout}{An optional \code{\link[=grid_layout]{grid_layout()}}.}

\item{row, col}{Cell (1-based) of the parent's layout to place into.}

\item{rowspan, colspan}{Number of cells to span.}

\item{mask}{An optional mask: a grob (or list of grobs), or an \code{\link[=as_mask]{as_mask()}}
result. The viewport's contents are rendered as an isolated layer and the
mask modulates their visibility.}

\item{alpha}{Optional group opacity in \verb{[0, 1]}. The viewport's contents are
composited as a single isolated layer at this opacity, so overlapping
elements do not accumulate (unlike per-element \code{gpar(alpha=)}). \code{NULL}
(default) means fully opaque.}

\item{name}{Optional name (for \code{\link[=edit_node]{edit_node()}}).}

\item{widths, heights}{Track sizes as a \code{\link[=unit]{unit()}} vector. Use \code{"null"} units for
flexible tracks that share leftover space in proportion to their value.}
}
\value{
A \code{viewport} object.

\code{grid_layout()}: a layout object.
}
\description{
A \code{viewport} is a rectangular region that establishes its own coordinate
systems (its \code{xscale}/\code{yscale} for \code{"native"} units), optionally rotated,
clipped, and carrying inheritable graphical parameters. Push one onto a scene
with \code{\link[=push]{push()}}. A viewport may define a row/column \code{\link[=grid_layout]{grid_layout()}}; child
viewports are then placed into cells via \code{row}/\code{col}.
}
\examples{
viewport(xscale = c(0, 10), yscale = c(0, 100))
}
