% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paint.R
\name{vl_pattern}
\alias{vl_pattern}
\title{Tiling-pattern fills}
\usage{
vl_pattern(
  grob,
  width = 0.1,
  height = 0.1,
  x = 0.5,
  y = 0.5,
  units = "npc",
  extend = "repeat"
)
}
\arguments{
\item{grob}{A grob, or a list of grobs, drawn into the tile (their \verb{0..1} npc
coordinates map to the tile, painted in order).}

\item{width, height}{Size of one tile cell (default \code{0.1} npc).}

\item{x, y}{Cell centre (default centred).}

\item{units}{Coordinate system for the geometry; see \code{\link[=linear_gradient]{linear_gradient()}}.}

\item{extend}{Tiling mode: \code{"repeat"} (default), \code{"reflect"}, or \code{"pad"}.
(SVG renders all modes as \code{repeat}.)}
}
\value{
A \code{vellum_pattern} object, suitable for \code{vl_gpar(fill = ...)}.
}
\description{
Create a pattern that fills a shape by tiling a grob. The grob is drawn once
into a tile occupying the unit square (\verb{0..1} npc), then repeated across a cell
of size \code{width} x \code{height} (in \code{units}) anchored at \verb{(x, y)}. Like gradients,
the cell geometry is resolved against the viewport at draw time.
}
\details{
The tile is rendered to a raster image (sized from \code{width}/\code{height} at the
scene's resolution) and embedded: PNG raster, SVG \verb{<image>} in a \verb{<pattern>}.
The PDF backend has no image support yet, so a pattern degrades to the tile's
average colour there.
}
\examples{
dots <- circle_grob(r = 0.25, gp = vl_gpar(fill = "white", col = NA))
vl_pattern(dots, width = 0.08, height = 0.08)
}
