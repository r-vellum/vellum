% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datashade.R
\name{datashade}
\alias{datashade}
\title{Aggregate-then-shade a large point cloud (datashader-style)}
\usage{
datashade(
  x,
  y,
  weight = NULL,
  width = 600L,
  height = 400L,
  xlim = NULL,
  ylim = NULL,
  category = NULL,
  colors = c("#deebf7", "#08306b"),
  how = c("eq_hist", "log", "cbrt", "linear"),
  span = NULL,
  clip = NULL,
  spread = NULL,
  interpolate = FALSE,
  name = NULL,
  vp = NULL,
  id = NULL,
  role = NULL
)
}
\arguments{
\item{x, y}{Point coordinates (plain numerics, in data space).}

\item{weight}{Optional per-point weight; cells accumulate the summed weight
instead of a plain count. \code{NULL} counts. A scalar is recycled to every
point; otherwise \code{weight} must be the same length as \code{x}.}

\item{width, height}{Aggregation grid size in cells (= output raster pixels).}

\item{xlim, ylim}{Data range to bin over; default the finite range of \code{x}/\code{y}.}

\item{category}{Optional per-point category (factor or vector) selecting
categorical (\code{count_cat}) shading; see Details. \code{NULL} (default) shades by
plain density.}

\item{colors}{For density shading, two or more colours forming the low-to-high
ramp. For categorical shading (\code{category} set), a per-category hue vector —
named by category level, or one colour per level in level order.}

\item{how}{Density-to-colour mapping: \code{"eq_hist"} (histogram equalisation —
datashader's default, reveals structure across orders of magnitude), \code{"log"},
\code{"cbrt"} (cube root), or \code{"linear"}. Also drives the per-cell opacity under
categorical shading.}

\item{span}{Optional \code{c(lo, hi)} density values mapped to the ends of the
colour ramp / opacity range; densities outside are clamped. \code{NULL} (default)
uses the full observed range.}

\item{clip}{Optional percentile pair in \verb{[0, 1]} (e.g. \code{c(0.01, 0.99)}) deriving
\code{span} from the quantiles of the non-empty cell densities — a robust way to
keep a few extreme cells from flattening the rest. Overrides \code{span}.}

\item{spread}{Optional post-aggregation spreading, applied to the shaded
raster to keep sparse output visible (see \code{\link[=spread]{spread()}} / \code{\link[=dynspread]{dynspread()}}):
\code{NULL} (default) none; a positive integer applies \code{\link[=spread]{spread()}} with that pixel
radius; \code{"auto"} applies \code{\link[=dynspread]{dynspread()}} (radius chosen from the image density).}

\item{interpolate}{Passed to \code{\link[=raster_grob]{raster_grob()}}; \code{FALSE} keeps hard bin edges.}

\item{name, vp, id, role}{Passed to \code{\link[=raster_grob]{raster_grob()}} (see \link{grob}).}
}
\value{
A \link{grob} (a raster), drawable with \code{\link[=draw]{draw()}}.
}
\description{
For data beyond the point where drawing one marker each is practical
(overplotted, millions of points), the fast and \emph{overplotting-honest} approach
is not to draw markers faster but to \strong{not draw markers at all}: bin the points
into a canvas-sized grid in one pass, then colour each cell by its density. This
is what makes \href{https://datashader.org}{datashader} fast — aggregation decouples
cost from both point count and overplotting. \code{datashade()} returns a single
\code{\link[=raster_grob]{raster_grob()}} you draw like any other grob.
}
\details{
The points are binned over \code{xlim} x \code{ylim} into a \code{width} x \code{height} grid, so to
line the image up with data axes draw it inside a \code{\link[=vl_viewport]{vl_viewport()}} whose \code{xscale} /
\code{yscale} match \code{xlim} / \code{ylim} (it fills the viewport, npc \verb{0..1}). For crisp
bins make \code{width}/\code{height} match the viewport's pixel size and keep
\code{interpolate = FALSE}.
}
\section{Categorical shading (\code{count_cat})}{
Pass \code{category} (a factor or vector, one value per point) to shade by \emph{category}
rather than plain density: each category is aggregated into its own count grid in
the same single pass, and each cell is coloured by the \strong{count-weighted average}
of the category hues it contains, with opacity driven by the cell's total density
(via \code{how}). This reveals which category dominates where \emph{and} where categories
mix, without overplotting bias. When \code{category} is set, \code{colors} is a per-category
hue vector (named by level, or one colour per level in level order) instead of a
low-to-high ramp.
}

\examples{
set.seed(1)
n <- 1e6
x <- rnorm(n); y <- x * 0.5 + rnorm(n)
g <- datashade(x, y, width = 400, height = 300)
s <- vl_scene(6, 4.5) |>
  push(vl_viewport(xscale = range(x), yscale = range(y))) |>
  draw(g)

# Categorical: colour each cell by which group dominates it
grp <- sample(c("a", "b"), n, replace = TRUE)
gc <- datashade(x, y, category = grp, colors = c(a = "#e41a1c", b = "#377eb8"))
}
\seealso{
\code{\link[=datashade_lines]{datashade_lines()}} and \code{\link[=datashade_segments]{datashade_segments()}} for the line/segment
(dense-timeseries, network-edge) counterparts, and \code{\link[=dynspread]{dynspread()}}/\code{\link[=spread]{spread()}}.
}
