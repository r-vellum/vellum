% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serialize.R
\name{scene_write}
\alias{scene_write}
\alias{scene_read}
\title{Write and read a scene}
\usage{
scene_write(scene, path)

scene_read(path)
}
\arguments{
\item{scene}{A \code{\link[=vl_scene]{vl_scene()}}, or anything with an \code{\link[=as_vellum_scene]{as_vellum_scene()}} method.}

\item{path}{File path. The extension picks the format (\code{.rds} or \code{.json}).}
}
\value{
\code{scene_write()}: \code{path}, invisibly. \code{scene_read()}: a \code{vellum_scene}.
}
\description{
Persist a scene so it can be rebuilt later, in another session, or in another
process. \code{.rds} is the default and needs nothing extra; \code{.json} produces a
portable text format and needs the jsonlite package.
}
\details{
What this buys you: caching a built scene without re-running the code that
built it, sending one over a wire for a worker to render, and keeping a
reproducible intermediate between "the code" and "the pixels".
}
\examples{
s <- vl_scene(2, 2) |> draw(circle_grob(gp = vl_gpar(fill = "steelblue")))
f <- tempfile(fileext = ".rds")
scene_write(s, f)
identical(as_scene_spec(scene_read(f)), as_scene_spec(s))
}
\seealso{
\code{\link[=as_scene_spec]{as_scene_spec()}}, \code{\link[=scene_diff]{scene_diff()}}, \code{\link[=scene_hash]{scene_hash()}}
}
