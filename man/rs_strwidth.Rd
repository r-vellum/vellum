% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{rs_strwidth}
\alias{rs_strwidth}
\alias{rs_strheight}
\title{Measure text}
\usage{
rs_strwidth(
  label,
  family = "",
  fontface = "plain",
  fontsize = 12,
  cex = 1,
  unit = "in"
)

rs_strheight(
  label,
  family = "",
  fontface = "plain",
  fontsize = 12,
  cex = 1,
  unit = "in"
)
}
\arguments{
\item{label}{A single string to measure.}

\item{family}{Font family (e.g. \code{"sans"}, \code{"serif"}, \code{"mono"}, or a specific
family name). \code{""} uses the system default.}

\item{fontface}{One of \code{"plain"}, \code{"bold"}, \code{"italic"}, \code{"bold.italic"}.}

\item{fontsize}{Font size in points.}

\item{cex}{Multiplier applied to \code{fontsize}.}

\item{unit}{Output unit: one of \code{"in"}, \code{"pt"}, \code{"mm"}, \code{"cm"}.}
}
\value{
The text width as a single number in \code{unit}.

\code{rs_strheight()}: the text height as a single number in \code{unit}.
}
\description{
Returns the rendered width of a string for layout purposes, using the same
shaping (\pkg{textshaping}/HarfBuzz + \pkg{systemfonts}) the renderer uses, so
measurements match drawn text. Device-independent (does not need an open scene).
}
