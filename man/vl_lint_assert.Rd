% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lint.R
\name{vl_lint_assert}
\alias{vl_lint_assert}
\title{Fail on lint findings}
\usage{
vl_lint_assert(
  scene,
  ...,
  severity = c("warning", "note"),
  on = c("error", "warn")
)
}
\arguments{
\item{scene}{A \code{\link[=vl_scene]{vl_scene()}}, or anything with an \code{\link[=as_vellum_scene]{as_vellum_scene()}} method.}

\item{...}{Passed to \code{\link[=vl_lint]{vl_lint()}} — \code{rules}, \code{exclude}, the thresholds.}

\item{severity}{\code{"warning"} (default) fails only on warnings; \code{"note"} fails
on anything at all.}

\item{on}{\code{"error"} (default) or \code{"warn"}.}
}
\value{
Invisibly, the findings that triggered the failure — a zero-row frame
when the scene is clean.
}
\description{
\code{vl_lint()} for a test suite or a CI job: run the rules and stop if anything
turns up. A figure that lints clean today stays that way, in the same spirit
as \code{\link[=font_pin]{font_pin()}} and \code{\link[=scene_hash]{scene_hash()}} — with the difference that this one
catches defects rather than changes.
}
\examples{
clean <- vl_scene(3, 2) |> draw(text_grob("fine", gp = vl_gpar(fontsize = 12)))
vl_lint_assert(clean)

# In a test:
# test_that("the figure has no lint findings", vl_lint_assert(my_plot()))
}
\seealso{
\code{\link[=vl_lint]{vl_lint()}}, \code{\link[=vl_lint_overlay]{vl_lint_overlay()}} to see what was reported.
}
