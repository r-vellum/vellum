% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unit.R
\name{vl_convert}
\alias{vl_convert}
\title{Convert a unit to another unit, in a scene's context}
\usage{
vl_convert(
  u,
  to = "mm",
  scene = NULL,
  name = NULL,
  axis = c("x", "y"),
  what = c("length", "position")
)
}
\arguments{
\item{u}{A \code{\link[=vl_unit]{vl_unit()}} vector (or a bare numeric, read as \code{npc}).}

\item{to}{Target unit: \code{"mm"} (default), \code{"cm"}, \code{"in"}, \code{"pt"}, \code{"px"},
\code{"npc"}, or \code{"native"}.}

\item{scene}{A \code{\link[=vl_scene]{vl_scene()}} (or anything with an \code{\link[=as_vellum_scene]{as_vellum_scene()}} method)
giving the context. Required unless every input \emph{and} \code{to} is absolute.}

\item{name}{Name of the viewport to resolve against. \code{NULL} (default) uses
the whole page.}

\item{axis}{Which extent relative units refer to: \code{"x"} (default) or \code{"y"}.}

\item{what}{\code{"length"} (default) for a size, \code{"position"} for a coordinate.
Only affects \code{native}.}
}
\value{
A numeric vector the same length as \code{u}, in \code{to} units.
}
\description{
The counterpart to grid's \code{convertWidth()}/\code{convertHeight()}/\code{convertX()}/
\code{convertY()}: resolve a \code{\link[=vl_unit]{vl_unit()}} vector to a plain number in some other
unit. \code{\link[=why_size]{why_size()}} explains a \emph{named node's} resolved size and
\code{\link[=scene_model]{scene_model()}} reports resolved boxes for \emph{keyed elements}; this answers the
remaining question — "how big is this particular unit, here?" — which a layer
built on vellum needs whenever it has to size something itself.
}
\details{
Absolute units (\code{mm}, \code{cm}, \verb{in}, \code{pt}) convert with no context, so \code{scene}
may be omitted. Relative units (\code{npc}, \code{native}) need to know the region they
are relative to, and therefore need a \code{scene} — and, for anything other than
the whole page, the \code{name} of a viewport in it.

Where grid uses four functions, this uses two arguments: \code{axis} picks the
x or y extent, and \code{what} distinguishes a \strong{length} from a \strong{position}.
They differ only for \code{native}, and only when the scale does not start at
zero: on \code{xscale = c(10, 20)}, \code{unit(12, "native")} is \emph{two tenths} of the
width as a position, but \emph{twelve tenths} as a length.
}
\examples{
s <- vl_scene(4, 3, dpi = 100)
vl_convert(vl_unit(1, "in"), "mm") # absolute: no scene needed
vl_convert(vl_unit(0.5, "npc"), "mm", s) # half the page width
vl_convert(vl_unit(0.5, "npc"), "mm", s, axis = "y")

# A named viewport, and the length/position distinction on a shifted scale.
s2 <- s |> push(vl_viewport(width = 0.5, xscale = c(10, 20), name = "panel"))
vl_convert(vl_unit(12, "native"), "mm", s2, name = "panel")
vl_convert(vl_unit(12, "native"), "mm", s2, name = "panel", what = "position")
}
\seealso{
\code{\link[=why_size]{why_size()}}, \code{\link[=scene_model]{scene_model()}}, \code{\link[=vl_unit]{vl_unit()}}
}
