% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_stroke_to_path_var}
\alias{rs_stroke_to_path_var}
\title{Flatten a quadratic segment into line segments at roughly pixel accuracy.
Expand a stroke whose width varies along it into a fillable outline.}
\usage{
rs_stroke_to_path_var(x, y, nper, closed, hw, cap, join, miter, arc)
}
\description{
Input and output are device pixels, and the return uses the same flat
encoding as \code{\link{rs_stroke_to_path}}: \code{c(n_subpaths, len1, len2, ..., x..., y...)}.
}
\details{
\code{hw} is the \strong{half}-width at each vertex, parallel to \code{x}/\code{y}, so the caller
owns every question about how a width profile is positioned along the line.
\code{arc <= 0} means "auto" (about one point per pixel of arc).

This is a separate entry point rather than a flag on \code{rs_stroke_to_path}
because the two are genuinely different generators -- tiny-skia's stroker
versus our own offsetter (see \code{ribbon.rs}). Keeping them apart means a scene
with no width profile takes byte-for-byte the path it always took.
}
\keyword{internal}
