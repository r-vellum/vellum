% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serialize.R
\name{scene_diff}
\alias{scene_diff}
\title{What changed between two scenes}
\usage{
scene_diff(a, b, max_depth = 40L)
}
\arguments{
\item{a, b}{Two scenes (\code{\link[=vl_scene]{vl_scene()}}, or anything with an
\code{\link[=as_vellum_scene]{as_vellum_scene()}} method).}

\item{max_depth}{Stop descending after this many levels (guards a runaway
report on deeply nested trees).}
}
\value{
A data frame with columns \code{path} (where in the tree), \code{change}
(\code{"added"}, \code{"removed"}, or \code{"changed"}), and \code{detail}. Empty when the two
scenes are equivalent. Printing shows a readable summary.
}
\description{
Compares two scenes structurally and reports the differences in terms of the
scene — "the node \code{axis-x} moved", "3 marks changed fill" — rather than as a
pixel diff.
}
\details{
This is a better basis for visual-regression testing than comparing rendered
images, for one specific reason: an image diff is sensitive to the font stack,
so the same code compared across two machines produces differences that swamp
the real change. A structural diff compares what was \emph{asked for}, and is
unaffected.
}
\examples{
a <- vl_scene(3, 2) |> draw(circle_grob(r = 0.3, gp = vl_gpar(fill = "red")))
b <- vl_scene(3, 2) |> draw(circle_grob(r = 0.4, gp = vl_gpar(fill = "blue")))
scene_diff(a, b)
}
\seealso{
\code{\link[=scene_hash]{scene_hash()}} for a yes/no answer, \code{\link[=as_scene_spec]{as_scene_spec()}}.
}
