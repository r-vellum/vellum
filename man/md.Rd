% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{md}
\alias{md}
\title{Rich-text labels (markdown subset)}
\usage{
md(text)
}
\arguments{
\item{text}{A markup string (or a character vector for per-element labels).}
}
\value{
A \code{vellum_md_label} (length-1 \code{text}) or a list of them (length > 1).
}
\description{
\code{md()} builds a styled label from a small markdown/HTML-free subset, for use as
the \code{label} of \code{\link[=text_grob]{text_grob()}} (and anywhere a label is measured with
\code{\link[=grobwidth]{grobwidth()}}/\code{\link[=grobheight]{grobheight()}}). The base font/size/colour come from \code{gp}; markup
spans override per run.
}
\details{
Supported markup:
\itemize{
\item \verb{**bold**}
\item \verb{*italic*} or \verb{_italic_}
\item \verb{^sup^} (superscript) and \verb{~sub~} (subscript)
\item \verb{[text]\{#c00\}} — a coloured span (any R colour: name or hex)
}

Spans nest (e.g. \verb{**a^2^**}). \code{md()} with no markup is equivalent to the plain
string. Embedded newlines (\verb{\\n}) start a new line (stacked baseline-to-baseline).

\code{md()} is vectorised: a length-1 input returns a single \code{vellum_md_label}; a
longer vector returns a list of them (one per element), so a \code{vellumplot} mark can
carry a per-datum rich label.
}
\examples{
lab <- md("R^2^ = **0.91**")
labs <- md(c("*a*", "**b**")) # a list of two labels
}
