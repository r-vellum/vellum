% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spread.R
\name{dynspread}
\alias{dynspread}
\title{Dynamically spread a raster grob to a target density}
\usage{
dynspread(
  grob,
  max_px = 3L,
  threshold = 0.5,
  shape = c("circle", "square"),
  how = c("over", "add")
)
}
\arguments{
\item{grob}{A raster \link{grob} (e.g. from \code{\link[=datashade]{datashade()}}). Other grob kinds
are returned unchanged.}

\item{max_px}{Largest spread radius to consider (non-negative integer).}

\item{threshold}{Target fraction in \verb{(0, 1]} of non-empty pixels that should
have a non-empty neighbour; growth stops once it is reached.}

\item{shape}{Neighbourhood shape: \code{"circle"} (default, Euclidean radius) or
\code{"square"} (Chebyshev radius).}

\item{how}{How overlapping spread pixels combine: \code{"over"} (default) keeps the
colour of the most-opaque contributor; \code{"add"} accumulates opacity (clamped)
so overlaps darken.}
}
\value{
A raster \link{grob} with the chosen spread applied.
}
\description{
\href{https://datashader.org}{datashader's} \code{dynspread}: pick the smallest spread
radius (up to \code{max_px}) at which the shaded pixels become "connected enough" —
the fraction of non-empty pixels with a non-empty neighbour reaches \code{threshold}
— then \code{\link[=spread]{spread()}} by it. Denser images spread less, sparse ones more, so a mix
of dense and sparse regions all stay legible.
}
\examples{
set.seed(1)
g <- datashade_segments(rnorm(2000), rnorm(2000), rnorm(2000), rnorm(2000))
g2 <- dynspread(g)
}
\seealso{
\code{\link[=spread]{spread()}}, \code{\link[=datashade]{datashade()}}, \code{\link[=datashade_lines]{datashade_lines()}}.
}
