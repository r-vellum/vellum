% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output.R
\name{pdf_pages}
\alias{pdf_pages}
\title{Write several scenes as the pages of one PDF}
\usage{
pdf_pages(scenes, path = NULL)
}
\arguments{
\item{scenes}{A list of scenes (each a \code{\link[=vl_scene]{vl_scene()}} or anything with an
\code{\link[=as_vellum_scene]{as_vellum_scene()}} method).}

\item{path}{Output file. \code{NULL} returns the bytes instead.}
}
\value{
\code{path} invisibly, or a raw vector when \code{path} is \code{NULL}.
}
\description{
\code{render()} writes one page. This writes a document: a report's worth of
figures, one facet per page, an animation as a contact sheet.
}
\details{
Pages may differ in size — each carries its own page box — so a landscape
figure can sit between two portrait ones.

Tagging follows each page's own metadata, exactly as it does for a
single-page render (see \code{vignette("accessible-output")}): the pages are drawn
by the same code, so a document cannot drift from a single-page file.
}
\examples{
pages <- lapply(c("tomato", "steelblue", "seagreen"), function(col) {
  vl_scene(4, 3, dpi = 96, bg = "white") |>
    draw(circle_grob(r = 0.3, gp = vl_gpar(fill = col)))
})
f <- tempfile(fileext = ".pdf")
pdf_pages(pages, f)
file.size(f) > 0
}
\seealso{
\code{\link[=render]{render()}} for a single page.
}
