% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lint.R
\name{vl_lint_overlay}
\alias{vl_lint_overlay}
\title{Draw lint findings onto the scene}
\usage{
vl_lint_overlay(scene, findings = NULL, labels = TRUE)
}
\arguments{
\item{scene}{A \code{\link[=vl_scene]{vl_scene()}}, or anything with an \code{\link[=as_vellum_scene]{as_vellum_scene()}} method.}

\item{findings}{The result of \code{\link[=vl_lint]{vl_lint()}}. \code{NULL} (default) lints \code{scene}.}

\item{labels}{Whether to write the rule name beside each box.}
}
\value{
A new scene: \code{scene} with the overlay drawn on top, at page level.
}
\description{
Puts a box around every finding that has geometry, and labels it with the
rules that fired. For a graphics linter this is usually the fastest way to
understand a report: a message says a mark is clipped, an outline shows you
which one.
}
\details{
Findings with no geometry (a \code{rule_error}, or a rule reporting something
scene-wide) have nothing to point at and are skipped.
}
\examples{
s <- vl_scene(3, 2) |>
  draw(text_grob("off the edge", x = 1.6, y = 0.5)) |>
  draw(text_grob("tiny", x = 0.5, y = 0.2, gp = vl_gpar(fontsize = 2)))
# display(vl_lint_overlay(s))
nrow(vl_lint(s)) > 0
}
\seealso{
\code{\link[=vl_lint]{vl_lint()}}, \code{\link[=vl_lint_assert]{vl_lint_assert()}}
}
